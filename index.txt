<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1.0,maximum-scale=1.0,user-scalable=no">
<meta name="mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="apple-mobile-web-app-title" content="SwapSkill">
<meta name="theme-color" content="#060608">
<meta name="description" content="Swap skills with people around you. Teach what you know, learn what you love.">
<link rel="manifest" href="manifest.json">
<!-- Apple touch icons -->
<link rel="apple-touch-icon" href="icon-192.png">
<title>SwapSkill</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<!-- Load fonts non-blocking so splash appears instantly -->
<link rel="preload" href="https://fonts.googleapis.com/css2?family=Syne:wght@600;700;800&family=DM+Sans:ital,wght@0,300;0,400;0,500;0,600;1,400&display=swap" as="style" onload="this.onload=null;this.rel='stylesheet'">
<noscript><link href="https://fonts.googleapis.com/css2?family=Syne:wght@600;700;800&family=DM+Sans:ital,wght@0,300;0,400;0,500;0,600;1,400&display=swap" rel="stylesheet"></noscript>
<!-- Firebase must load synchronously before the app script -->
<script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-app-compat.js"></script>
<script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-auth-compat.js"></script>
<script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-firestore-compat.js"></script>
<!-- Restore theme before first paint to prevent flash -->
<script>
(function(){
  try{
    var t=localStorage.getItem('ss_theme');
    var el=document.documentElement;
    if(t==='light'){el.setAttribute('data-theme','light');}
    else{el.setAttribute('data-theme','dark');}
  }catch(e){}
})();
</script>
<style>
/* ── INSTANT SPLASH: shown before any JS runs, prevents black/white flash ── */
#instantSplash{
  position:fixed;inset:0;z-index:9999;
  background:#060608;
  display:flex;flex-direction:column;align-items:center;justify-content:center;gap:0;
  transition:opacity .5s ease;
  will-change:opacity;
}
/* Match whatever theme is saved in localStorage — JS sets data-theme before paint */
[data-theme="light"] #instantSplash{background:#f5f4f0}
#instantSplash.fade-out{opacity:0;pointer-events:none}
#instantSplash.hidden{display:none}
.is-orbs{display:flex;align-items:center;gap:12px;margin-bottom:22px}
.is-orb{border-radius:14px;display:flex;align-items:center;justify-content:center}
.is-o1{width:50px;height:50px;background:rgba(255,92,53,.12);border:1.5px solid rgba(255,92,53,.3)}
.is-o2{width:66px;height:66px;background:rgba(255,255,255,.03);border:1.5px solid #28283a}
.is-o3{width:50px;height:50px;background:rgba(0,201,167,.10);border:1.5px solid rgba(0,201,167,.28)}
.is-brand{font-family:'Syne','Arial Black',sans-serif;font-size:42px;font-weight:800;letter-spacing:-2.5px;color:#f0f0f4;line-height:1}
.is-brand span{color:#ff5c35}
.is-tag{font-size:12px;color:#505060;margin-top:9px;letter-spacing:.3px}
.is-ring{width:30px;height:30px;border:2.5px solid #1e1e26;border-top-color:#ff5c35;border-radius:50%;animation:isSpin .85s linear infinite;margin-top:40px}
@keyframes isSpin{to{transform:rotate(360deg)}}

/* ═══════════════════════════════════════
   CSS DESIGN TOKENS
═══════════════════════════════════════ */
:root {
  /* Dark theme defaults */
  --bg: #060608;
  --bg2: #0d0d10;
  --surf: #111115;
  --card: #16161a;
  --card2: #1e1e24;
  --card3: #252530;

  --accent: #ff5c35;
  --accent2: #ff7a55;
  --adim: rgba(255,92,53,0.10);
  --adim2: rgba(255,92,53,0.16);
  --aborder: rgba(255,92,53,0.30);

  --teal: #00c9a7;
  --tdim: rgba(0,201,167,0.10);
  --tborder: rgba(0,201,167,0.28);

  --blue: #4f8ef7;
  --green: #27ae60;
  --yellow: #f0b429;
  --purple: #9b7fe8;
  --red: #e74c3c;

  --txt: #f0f0f4;
  --txt2: #9090a0;
  --txt3: #505060;
  --txt4: #303040;

  --bdr: #1e1e26;
  --bdr2: #28283a;
  --bdr3: #323245;

  --nav-h: 64px;
  --topbar-h: 56px;
  --r: 18px;
  --r2: 13px;
  --r3: 10px;
  --r4: 8px;

  --shadow: 0 2px 12px rgba(0,0,0,0.4);
  --shadow2: 0 8px 32px rgba(0,0,0,0.5);
  --glow-accent: 0 0 24px rgba(255,92,53,0.22);
}

[data-theme="light"] {
  --bg: #f5f4f0;
  --bg2: #eeece8;
  --surf: #e8e6e0;
  --card: #ffffff;
  --card2: #f7f5f0;
  --card3: #eeece8;

  --adim: rgba(255,92,53,0.07);
  --adim2: rgba(255,92,53,0.12);
  --aborder: rgba(255,92,53,0.25);
  --tdim: rgba(0,201,167,0.08);
  --tborder: rgba(0,201,167,0.22);

  --txt: #16161a;
  --txt2: #5a5a70;
  --txt3: #9090a8;
  --txt4: #c0c0d0;

  --bdr: #dddbd5;
  --bdr2: #d0cec8;
  --bdr3: #c4c2bb;

  --shadow: 0 2px 12px rgba(0,0,0,0.08);
  --shadow2: 0 8px 32px rgba(0,0,0,0.12);
  --glow-accent: 0 0 24px rgba(255,92,53,0.14);
}

/* ═══ RESET ═══ */
*,*::before,*::after{margin:0;padding:0;box-sizing:border-box;-webkit-tap-highlight-color:transparent}
html,body{height:100%;background:var(--bg);color:var(--txt);font-family:'DM Sans',sans-serif;overscroll-behavior:none;overflow:hidden;-webkit-font-smoothing:antialiased}
input,button,textarea,select{font-family:inherit;color:inherit}
button{cursor:pointer;border:none;background:none;outline:none}
img{display:block;max-width:100%}
a{text-decoration:none;color:inherit}

/* ═══ SHELL ═══ */
#app{position:fixed;inset:0;max-width:430px;margin:0 auto;background:var(--bg);overflow:hidden;display:flex;flex-direction:column}

/* ═══ SCREENS ═══ */
.scr{position:absolute;inset:0;display:flex;flex-direction:column;opacity:0;pointer-events:none;transition:opacity .22s ease,transform .22s ease;transform:translateY(10px);background:var(--bg)}
.scr.on{opacity:1;pointer-events:all;transform:translateY(0)}

/* ═══════════════════════════════════════
   SPLASH
═══════════════════════════════════════ */
#sSplash{align-items:center;justify-content:center;background:var(--bg)}
.splash-inner{display:flex;flex-direction:column;align-items:center;gap:0}
.splash-orbs{display:flex;align-items:center;gap:12px;margin-bottom:24px}
.orb{border-radius:16px;display:flex;align-items:center;justify-content:center;animation:orbFloat 2.6s ease-in-out infinite}
.orb1{width:52px;height:52px;background:var(--adim2);border:1.5px solid var(--aborder);animation-delay:0s}
.orb2{width:68px;height:68px;background:rgba(255,255,255,0.03);border:1.5px solid var(--bdr2);animation-delay:.2s}
.orb3{width:52px;height:52px;background:var(--tdim);border:1.5px solid var(--tborder);animation-delay:.4s}
@keyframes orbFloat{0%,100%{transform:translateY(0)}50%{transform:translateY(-8px)}}
.brand{font-family:'Syne',sans-serif;font-size:44px;font-weight:800;letter-spacing:-2.5px;color:var(--txt);line-height:1}
.brand span{color:var(--accent)}
.brand-tag{font-size:12px;color:var(--txt3);margin-top:9px;letter-spacing:.3px}
.splash-ring{width:32px;height:32px;border:2.5px solid var(--bdr2);border-top-color:var(--accent);border-radius:50%;animation:spin .9s linear infinite;margin-top:44px}

/* ═══════════════════════════════════════
   AUTH
═══════════════════════════════════════ */
#sAuth{overflow-y:auto;background:var(--bg)}
.auth-glow-a{position:absolute;width:340px;height:340px;border-radius:50%;background:radial-gradient(circle,var(--adim) 0%,transparent 70%);top:-100px;left:-80px;pointer-events:none;animation:glowPulse 9s ease-in-out infinite alternate}
.auth-glow-b{position:absolute;width:280px;height:280px;border-radius:50%;background:radial-gradient(circle,var(--tdim) 0%,transparent 70%);bottom:-60px;right:-50px;pointer-events:none;animation:glowPulse 11s ease-in-out infinite alternate-reverse}
@keyframes glowPulse{from{transform:scale(1)}to{transform:scale(1.4) translate(18px,12px)}}
.auth-body{flex:1;display:flex;flex-direction:column;align-items:center;justify-content:center;padding:36px 24px 28px;position:relative;z-index:1;width:100%}
.auth-logo-area{text-align:center;margin-bottom:28px}
.auth-orbs{display:flex;align-items:center;justify-content:center;gap:10px;margin-bottom:14px}
.auth-card{background:var(--card);border:1px solid var(--bdr2);border-radius:22px;padding:24px 20px;width:100%;box-shadow:var(--shadow2)}
.tab-row{display:flex;background:var(--surf);border-radius:12px;padding:3px;gap:3px;margin-bottom:22px}
.auth-tab{flex:1;padding:9px 6px;text-align:center;border-radius:10px;font-size:13px;font-weight:600;color:var(--txt2);cursor:pointer;transition:all .2s}
.auth-tab.on{background:var(--accent);color:#fff}
.aform{display:none}
.aform.on{display:block}
.field{margin-bottom:12px}
.field-lbl{font-size:10px;font-weight:700;color:var(--txt3);text-transform:uppercase;letter-spacing:1px;margin-bottom:5px}
.inp{width:100%;background:var(--surf);border:1.5px solid var(--bdr2);border-radius:12px;padding:13px 14px;font-size:14px;color:var(--txt);outline:none;transition:border-color .18s,box-shadow .18s;-webkit-appearance:none}
.inp:focus{border-color:var(--accent);box-shadow:0 0 0 3px var(--adim)}
.inp::placeholder{color:var(--txt3)}
.auth-err{background:var(--adim);border:1px solid var(--aborder);border-radius:10px;padding:10px 14px;font-size:12.5px;color:var(--accent2);margin-bottom:12px;display:none;line-height:1.45}
.auth-err.on{display:block}
.divider{display:flex;align-items:center;gap:10px;margin:16px 0}
.divider-line{flex:1;height:1px;background:var(--bdr)}
.divider-txt{font-size:11px;color:var(--txt3);font-weight:600}

/* ═══ BUTTONS ═══ */
.btn{display:flex;align-items:center;justify-content:center;gap:8px;border-radius:var(--r2);font-weight:700;transition:all .18s;cursor:pointer;position:relative;overflow:hidden;border:none;outline:none}
.btn::after{content:'';position:absolute;inset:0;background:rgba(255,255,255,0);transition:background .15s}
.btn:active::after{background:rgba(255,255,255,0.08)}
.btn-primary{width:100%;padding:14px;background:var(--accent);color:#fff;font-family:'Syne',sans-serif;font-size:14px;font-weight:700;letter-spacing:.2px}
.btn-primary:hover{transform:translateY(-1px);box-shadow:var(--glow-accent)}
.btn-primary:active{transform:translateY(0);box-shadow:none}
.btn-secondary{width:100%;padding:13px;background:var(--surf);border:1.5px solid var(--bdr2);color:var(--txt2);font-size:13px;font-weight:600;margin-top:8px}
.btn-secondary:hover{background:var(--card2);border-color:var(--bdr3);color:var(--txt)}
.btn-ghost{width:100%;padding:13px;background:transparent;border:1.5px solid var(--bdr2);color:var(--txt2);font-size:13px;font-weight:600;margin-top:8px}
.btn-ghost:hover{border-color:var(--aborder);color:var(--accent)}
.btn-teal{background:var(--teal);color:#000}
.btn-teal:hover{transform:translateY(-1px);box-shadow:0 8px 20px rgba(0,201,167,0.3)}
.btn-danger{background:var(--red);color:#fff}
.btn-spinner{width:17px;height:17px;border:2px solid rgba(255,255,255,0.28);border-top-color:#fff;border-radius:50%;animation:spin .75s linear infinite}
@keyframes spin{to{transform:rotate(360deg)}}

/* ═══════════════════════════════════════
   TOPBAR
═══════════════════════════════════════ */
.topbar{display:flex;align-items:center;justify-content:space-between;padding:0 16px;height:var(--topbar-h);background:var(--bg);border-bottom:1px solid var(--bdr);flex-shrink:0;z-index:10;backdrop-filter:blur(20px)}
.t-brand{font-family:'Syne',sans-serif;font-size:19px;font-weight:800;letter-spacing:-.8px;color:var(--txt)}
.t-brand span{color:var(--accent)}
.t-right{display:flex;gap:6px;align-items:center}
.icon-btn{width:36px;height:36px;background:var(--card);border:1px solid var(--bdr2);border-radius:11px;display:flex;align-items:center;justify-content:center;cursor:pointer;transition:all .16s;position:relative;flex-shrink:0}
.icon-btn:hover{border-color:var(--aborder);background:var(--card2)}
.notif-dot{position:absolute;top:8px;right:8px;width:6px;height:6px;background:var(--accent);border-radius:50%;border:1.5px solid var(--bg);display:none}

/* ═══ PWA INSTALL BANNER ═══ */
#pwaInstallBanner{position:fixed;bottom:0;left:0;right:0;z-index:9999;max-width:430px;margin:0 auto;background:var(--card);border-top:1px solid var(--aborder);padding:16px 18px 28px;display:flex;align-items:center;gap:13px;box-shadow:0 -6px 32px rgba(0,0,0,0.45);transform:translateY(100%);transition:transform .35s cubic-bezier(.22,1,.36,1)}
#pwaInstallBanner.show{transform:translateY(0)}
.pwa-icon{width:48px;height:48px;border-radius:14px;background:var(--adim2);border:1.5px solid var(--aborder);display:flex;align-items:center;justify-content:center;flex-shrink:0}
.pwa-text{flex:1;min-width:0}
.pwa-title{font-family:'Syne',sans-serif;font-size:14px;font-weight:700;color:var(--txt);margin-bottom:2px}
.pwa-sub{font-size:11.5px;color:var(--txt2);line-height:1.4}
.pwa-actions{display:flex;flex-direction:column;gap:7px;flex-shrink:0}
.pwa-btn-install{padding:9px 16px;background:var(--accent);color:#fff;border:none;border-radius:10px;font-size:13px;font-weight:700;font-family:'Syne',sans-serif;cursor:pointer;white-space:nowrap}
.pwa-btn-dismiss{padding:7px 16px;background:transparent;color:var(--txt3);border:1px solid var(--bdr2);border-radius:10px;font-size:12px;font-weight:600;cursor:pointer;white-space:nowrap}

/* ═══ NOTIF SWAP MSG ═══ */
.notif-swap-msg{font-size:12px;color:var(--txt2);background:var(--surf);border-left:2.5px solid var(--accent);border-radius:0 6px 6px 0;padding:5px 9px;margin:5px 0 3px;font-style:italic;line-height:1.4}

/* ═══════════════════════════════════════
   BOTTOM NAV
═══════════════════════════════════════ */
.bnav{position:absolute;bottom:0;left:0;right:0;height:var(--nav-h);background:var(--bg);border-top:1px solid var(--bdr);display:grid;grid-template-columns:1fr 1fr 1fr 1fr 1fr;align-items:end;padding-bottom:10px;z-index:20}
.ni{display:flex;flex-direction:column;align-items:center;gap:4px;cursor:pointer;padding:6px 0;transition:all .16s}
.ni-ic{width:34px;height:34px;border-radius:10px;display:flex;align-items:center;justify-content:center;transition:background .16s}
.ni.on .ni-ic{background:var(--adim)}
.ni-lbl{font-size:10px;color:var(--txt3);font-weight:600;letter-spacing:.1px;transition:color .16s}
.ni.on .ni-lbl{color:var(--accent)}
.ni-fab{display:flex;flex-direction:column;align-items:center;gap:4px;cursor:pointer;padding-bottom:0}
.ni-fab-btn{width:48px;height:48px;border-radius:15px;background:var(--accent);display:flex;align-items:center;justify-content:center;margin-top:-20px;box-shadow:0 6px 18px rgba(255,92,53,0.40);transition:all .18s}
.ni-fab-btn:hover{transform:scale(1.08) translateY(-2px);box-shadow:0 10px 28px rgba(255,92,53,0.50)}
.ni-fab-btn:active{transform:scale(0.97)}
.ni-fab-lbl{font-size:10px;color:var(--txt3);font-weight:600}
.ni-fab.on .ni-fab-lbl{color:var(--accent)}

/* ═══════════════════════════════════════
   HOME
═══════════════════════════════════════ */
#sHome{padding-bottom:var(--nav-h)}
.home-scroll{flex:1;overflow-y:auto;overflow-x:hidden}
.home-scroll::-webkit-scrollbar{display:none}
.home-hero{padding:20px 18px 22px;background:var(--bg2);border-bottom:1px solid var(--bdr);position:relative;overflow:hidden}
.home-hero::before{content:'';position:absolute;top:-50px;right:-30px;width:160px;height:160px;border-radius:50%;background:radial-gradient(circle,var(--adim) 0%,transparent 70%);pointer-events:none}
.hero-greeting{font-size:11px;color:var(--txt3);text-transform:uppercase;letter-spacing:1.8px;margin-bottom:4px}
.hero-name{font-family:'Syne',sans-serif;font-size:20px;font-weight:800;line-height:1.28;margin-bottom:14px;color:var(--txt)}
.hero-name span{color:var(--accent)}
.skill-chips{display:flex;gap:6px;flex-wrap:wrap}
.chip{background:var(--card);border:1px solid var(--bdr2);border-radius:20px;padding:5px 12px;font-size:11.5px;color:var(--txt2);font-weight:500}
.pad{padding:18px}
.sec-head{display:flex;align-items:center;justify-content:space-between;margin-bottom:12px}
.sec-title{font-family:'Syne',sans-serif;font-size:13.5px;font-weight:700;color:var(--txt)}
.sec-link{font-size:12px;color:var(--accent);font-weight:600;cursor:pointer;padding:2px 0}
/* Quick action grid */
.qa-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:22px}
.qa-card{padding:17px 15px;border-radius:var(--r);cursor:pointer;transition:transform .2s,box-shadow .2s;position:relative;overflow:hidden}
.qa-card:hover{transform:translateY(-3px);box-shadow:var(--shadow2)}
.qa-card:active{transform:translateY(0)}
.qa-teach{background:linear-gradient(135deg,#ff5c35,#cc3a18)}
.qa-learn{background:linear-gradient(135deg,#00c9a7,#007a67)}
.qa-ic{margin-bottom:10px}
.qa-title{font-family:'Syne',sans-serif;font-size:13px;font-weight:700;color:#fff;margin-bottom:2px}
.qa-sub{font-size:11px;color:rgba(255,255,255,0.55)}
.qa-arr{position:absolute;bottom:14px;right:14px;opacity:0.55}
/* Match horizontal scroll */
.match-scroll{display:flex;gap:10px;overflow-x:auto;padding-bottom:4px;margin-bottom:22px;scrollbar-width:none}
.match-scroll::-webkit-scrollbar{display:none}
.match-card{flex-shrink:0;width:180px;background:var(--card);border:1px solid var(--bdr2);border-radius:18px;padding:14px;cursor:pointer;transition:all .18s}
.match-card:hover{border-color:var(--aborder);transform:translateY(-2px);box-shadow:var(--shadow)}
.m-avatar{width:42px;height:42px;border-radius:13px;display:flex;align-items:center;justify-content:center;font-size:16px;font-weight:700;font-family:'Syne',sans-serif;color:#fff;margin-bottom:9px;flex-shrink:0}
.m-name{font-family:'Syne',sans-serif;font-weight:700;font-size:13px;margin-bottom:2px;color:var(--txt);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.m-teach{font-size:11px;color:var(--accent);font-weight:600;margin-bottom:1px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.m-learn{font-size:11px;color:var(--teal);font-weight:600;margin-bottom:9px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.m-bar-row{display:flex;align-items:center;gap:6px}
.m-pct{font-family:'Syne',sans-serif;font-size:11px;font-weight:700;color:var(--green)}
.m-bar{flex:1;height:3px;background:var(--bdr2);border-radius:2px;overflow:hidden}
.m-bar-fill{height:100%;background:linear-gradient(90deg,var(--green),#2ecc71)}
/* Stats */
.stats-row{background:var(--card);border:1px solid var(--bdr2);border-radius:18px;padding:18px;display:grid;grid-template-columns:1fr 1px 1fr 1px 1fr;text-align:center;box-shadow:var(--shadow)}
.s-divider{background:var(--bdr)}
.s-num{font-family:'Syne',sans-serif;font-size:20px;font-weight:800;color:var(--txt)}
.s-lbl{font-size:10px;color:var(--txt3);margin-top:3px;text-transform:uppercase;letter-spacing:.6px}

/* ═══════════════════════════════════════
   EXPLORE
═══════════════════════════════════════ */
#sExplore{padding-bottom:var(--nav-h)}
.exp-scroll{flex:1;overflow-y:auto;overflow-x:hidden}
.exp-scroll::-webkit-scrollbar{display:none}
.search-bar-wrap{padding:10px 16px;background:var(--bg);border-bottom:1px solid var(--bdr);flex-shrink:0}
.search-bar{display:flex;align-items:center;gap:9px;background:var(--card);border:1.5px solid var(--bdr2);border-radius:13px;padding:10px 13px;transition:border-color .18s}
.search-bar:focus-within{border-color:var(--accent)}
.search-bar input{flex:1;background:none;border:none;outline:none;font-size:14px;color:var(--txt)}
.search-bar input::placeholder{color:var(--txt3)}
.filter-chips{display:flex;gap:7px;overflow-x:auto;padding:10px 16px 4px;scrollbar-width:none;flex-shrink:0}
.filter-chips::-webkit-scrollbar{display:none}
.fchip{flex-shrink:0;padding:6px 13px;border-radius:20px;font-size:12px;font-weight:600;cursor:pointer;border:1.5px solid var(--bdr2);background:var(--card);color:var(--txt2);transition:all .16s;white-space:nowrap;display:flex;align-items:center;gap:6px}
.fchip.on{background:var(--accent);color:#fff;border-color:var(--accent)}
.fchip:not(.on):hover{border-color:var(--aborder);color:var(--accent)}
.exp-list{padding:12px 16px;display:flex;flex-direction:column;gap:10px}
/* Skill card */
.sk-card{background:var(--card);border:1px solid var(--bdr);border-radius:var(--r);padding:16px;cursor:pointer;transition:all .2s;position:relative;overflow:hidden}
.sk-card::before{content:'';position:absolute;top:0;left:0;right:0;height:2px;background:linear-gradient(90deg,var(--accent),var(--teal));transform:scaleX(0);transform-origin:left;transition:transform .28s}
.sk-card:hover{border-color:var(--bdr2);transform:translateY(-2px);box-shadow:var(--shadow)}
.sk-card:hover::before{transform:scaleX(1)}
.sk-top{display:flex;align-items:flex-start;gap:12px;margin-bottom:11px}
.sk-av{width:46px;height:46px;border-radius:13px;display:flex;align-items:center;justify-content:center;font-size:18px;font-weight:700;font-family:'Syne',sans-serif;color:#fff;flex-shrink:0}
.sk-name{font-family:'Syne',sans-serif;font-weight:700;font-size:14px;margin-bottom:3px;color:var(--txt)}
.sk-meta{font-size:11px;color:var(--txt3);display:flex;align-items:center;gap:6px;flex-wrap:wrap}
.badge{font-size:10px;padding:2px 7px;border-radius:6px;font-weight:700}
.badge-green{background:rgba(39,174,96,0.12);color:var(--green)}
.badge-teal{background:var(--tdim);color:var(--teal)}
.badge-blue{background:rgba(79,142,247,0.12);color:var(--blue)}
.swap-panel{display:grid;grid-template-columns:1fr 28px 1fr;align-items:center;gap:6px;background:var(--bg2);border:1px solid var(--bdr);border-radius:12px;padding:10px;margin-bottom:10px}
.swap-side{text-align:center}
.swap-side-lbl{font-size:9px;font-weight:700;text-transform:uppercase;letter-spacing:1px;margin-bottom:3px}
.swap-side-lbl.teach{color:var(--accent)}
.swap-side-lbl.learn{color:var(--teal)}
.swap-side-val{font-size:12px;font-weight:700;color:var(--txt)}
.swap-ic{width:28px;height:28px;background:var(--card3);border-radius:50%;display:flex;align-items:center;justify-content:center;border:1px solid var(--bdr2)}
.sk-footer{display:flex;align-items:center;justify-content:space-between}
.rating-row{font-size:12px;color:var(--txt3);display:flex;align-items:center;gap:5px}
.rating-star{color:var(--yellow)}
.swap-btn{padding:8px 15px;background:var(--accent);border-radius:10px;color:#fff;font-size:12px;font-weight:700;display:flex;align-items:center;gap:6px;transition:all .16s;cursor:pointer;border:none;outline:none}
.swap-btn:hover{background:var(--accent2);transform:translateY(-1px);box-shadow:var(--glow-accent)}
.swap-btn:active{transform:translateY(0)}

/* ═══════════════════════════════════════
   DETAIL
═══════════════════════════════════════ */
#sDetail{overflow-y:auto}
.back-bar{padding:12px 16px;display:flex;align-items:center;gap:8px;cursor:pointer;flex-shrink:0;border-bottom:1px solid var(--bdr);background:var(--bg)}
.back-bar span{font-size:14px;color:var(--txt2);font-weight:500}
.det-inner{overflow-y:auto;flex:1}
.det-hero{padding:22px 18px;text-align:center;border-bottom:1px solid var(--bdr);background:var(--bg2)}
.det-av{width:76px;height:76px;border-radius:22px;display:flex;align-items:center;justify-content:center;font-size:28px;font-weight:700;font-family:'Syne',sans-serif;color:#fff;margin:0 auto 12px}
.det-name{font-family:'Syne',sans-serif;font-size:19px;font-weight:800;margin-bottom:8px;color:var(--txt)}
.det-badges{display:flex;align-items:center;justify-content:center;gap:8px;flex-wrap:wrap;margin-bottom:14px}
.det-stats{display:flex;gap:16px;justify-content:center}
.det-stat{text-align:center}
.det-stat-n{font-family:'Syne',sans-serif;font-size:18px;font-weight:800;color:var(--txt)}
.det-stat-l{font-size:10px;color:var(--txt3);text-transform:uppercase;letter-spacing:.5px}
.det-body{padding:18px}
.det-box{background:var(--card);border:1px solid var(--bdr2);border-radius:var(--r);padding:16px;margin-bottom:12px;box-shadow:var(--shadow)}
.det-box-title{font-family:'Syne',sans-serif;font-size:12px;font-weight:700;color:var(--txt2);text-transform:uppercase;letter-spacing:.8px;margin-bottom:12px}
.det-swap-grid{display:grid;grid-template-columns:1fr 28px 1fr;align-items:center;gap:6px}
.det-swap-cell{background:var(--bg2);border:1px solid var(--bdr);border-radius:11px;padding:11px;text-align:center}
.det-cell-lbl{font-size:9px;font-weight:700;text-transform:uppercase;letter-spacing:1px;margin-bottom:4px}
.det-cell-val{font-size:12.5px;font-weight:700;color:var(--txt)}
.det-bio{font-size:13px;color:var(--txt2);line-height:1.6}

/* ═══════════════════════════════════════
   SWAPS
═══════════════════════════════════════ */
#sSwaps{padding-bottom:var(--nav-h)}
.sw-scroll{flex:1;overflow-y:auto;overflow-x:hidden}
.sw-scroll::-webkit-scrollbar{display:none}
.tabs-bar{display:flex;background:var(--card);border:1px solid var(--bdr2);border-radius:13px;padding:3px;gap:3px;margin:12px 16px}
.sw-tab{flex:1;padding:8px 4px;text-align:center;border-radius:10px;font-size:11px;font-weight:700;color:var(--txt3);cursor:pointer;transition:all .18s;white-space:nowrap}
.sw-tab.on{background:var(--accent);color:#fff}
.sw-list{padding:0 16px 16px;display:flex;flex-direction:column;gap:10px}
.sw-item{background:var(--card);border:1px solid var(--bdr2);border-radius:18px;padding:15px;box-shadow:var(--shadow);transition:border-color .16s}
.sw-item:hover{border-color:var(--bdr3)}
.sw-item-top{display:flex;align-items:center;gap:11px;margin-bottom:10px}
.sw-av{width:42px;height:42px;border-radius:12px;display:flex;align-items:center;justify-content:center;font-size:16px;font-weight:700;font-family:'Syne',sans-serif;color:#fff;flex-shrink:0}
.sw-info{flex:1;min-width:0}
.sw-user{font-family:'Syne',sans-serif;font-weight:700;font-size:13px;color:var(--txt);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.sw-meta{font-size:11px;color:var(--txt3);margin-top:1px}
.sw-badge{font-size:10.5px;font-weight:700;padding:3px 9px;border-radius:7px;flex-shrink:0}
.st-pending{background:rgba(240,180,41,0.14);color:var(--yellow)}
.st-accepted{background:rgba(39,174,96,0.14);color:var(--green)}
.st-completed{background:rgba(79,142,247,0.14);color:var(--blue)}
.st-declined{background:var(--adim);color:var(--accent)}
.sw-skills{display:flex;gap:8px;margin-bottom:10px}
.sw-skill{flex:1;background:var(--bg2);border:1px solid var(--bdr);border-radius:10px;padding:8px 9px;text-align:center}
.sw-skill-lbl{font-size:9px;font-weight:700;text-transform:uppercase;letter-spacing:.8px;margin-bottom:3px}
.sw-skill-lbl.t{color:var(--accent)}.sw-skill-lbl.l{color:var(--teal)}
.sw-skill-val{font-size:11px;font-weight:600;color:var(--txt)}
.sw-actions{display:flex;gap:7px}
.sw-act-btn{flex:1;padding:9px 6px;border-radius:10px;font-size:11px;font-weight:700;display:flex;align-items:center;justify-content:center;gap:5px;transition:all .16s;border:none;cursor:pointer;outline:none}
.sa-accept{background:rgba(39,174,96,0.10);color:var(--green);border:1px solid rgba(39,174,96,0.22)}
.sa-accept:hover{background:var(--green);color:#000}
.sa-decline{background:var(--adim);color:var(--accent);border:1px solid var(--aborder)}
.sa-decline:hover{background:var(--accent);color:#fff}
.sa-call{background:rgba(79,142,247,0.10);color:var(--blue);border:1px solid rgba(79,142,247,0.22)}
.sa-call:hover{background:var(--blue);color:#fff}
.sa-rate{background:rgba(240,180,41,0.10);color:var(--yellow);border:1px solid rgba(240,180,41,0.22)}
.sa-rate:hover{background:var(--yellow);color:#000}
.empty-state{text-align:center;padding:50px 20px}
.empty-icon{width:52px;height:52px;background:var(--card);border:1px solid var(--bdr2);border-radius:15px;display:flex;align-items:center;justify-content:center;margin:0 auto 14px}
.empty-title{font-family:'Syne',sans-serif;font-size:16px;font-weight:700;color:var(--txt3);margin-bottom:7px}
.empty-sub{font-size:13px;color:var(--txt4);line-height:1.5}

/* ═══════════════════════════════════════
   SETUP / POST SKILL
═══════════════════════════════════════ */
#sSetup{padding-bottom:var(--nav-h)}
.setup-scroll{flex:1;overflow-y:auto}
.setup-scroll::-webkit-scrollbar{display:none}
.setup-sec{background:var(--card);border:1px solid var(--bdr2);border-radius:var(--r);padding:16px;margin:0 16px 12px;box-shadow:var(--shadow)}
.setup-sec-title{font-family:'Syne',sans-serif;font-size:13px;font-weight:700;color:var(--txt);margin-bottom:13px;display:flex;align-items:center;gap:8px}
.pill-search{width:100%;background:var(--surf);border:1.5px solid var(--bdr2);border-radius:11px;padding:10px 13px;font-size:13px;color:var(--txt);outline:none;margin-bottom:10px;-webkit-appearance:none;transition:border-color .18s}
.pill-search:focus{border-color:var(--accent)}
.pill-search::placeholder{color:var(--txt3)}
.sel-count{font-size:12px;color:var(--txt3);margin-bottom:9px}
.sel-count.active{color:var(--green)}
.pills-wrap{display:flex;flex-wrap:wrap;gap:7px;max-height:200px;overflow-y:auto;padding-bottom:4px}
.pills-wrap::-webkit-scrollbar{width:0}
.pill{padding:7px 13px;border-radius:20px;background:var(--card2);border:1.5px solid var(--bdr2);font-size:12px;font-weight:600;color:var(--txt2);cursor:pointer;transition:all .14s;user-select:none}
.pill:hover{border-color:var(--bdr3);color:var(--txt)}
.pill.p-teach{background:var(--adim2);border-color:var(--aborder);color:var(--accent)}
.pill.p-learn{background:var(--tdim);border-color:var(--tborder);color:var(--teal)}
.pill.hidden{display:none}

/* ═══════════════════════════════════════
   LEADERBOARD
═══════════════════════════════════════ */
#sLboard{padding-bottom:var(--nav-h)}
.lb-scroll{flex:1;overflow-y:auto}
.lb-scroll::-webkit-scrollbar{display:none}
.lb-hero{background:var(--card);border:1px solid var(--bdr2);border-radius:var(--r);padding:20px;margin:14px 16px;text-align:center;position:relative;overflow:hidden;box-shadow:var(--shadow)}
.lb-hero::before{content:'';position:absolute;top:-40px;left:50%;transform:translateX(-50%);width:180px;height:180px;border-radius:50%;background:radial-gradient(circle,var(--adim),transparent 70%);pointer-events:none}
.lb-title{font-family:'Syne',sans-serif;font-size:17px;font-weight:800;color:var(--txt);position:relative;z-index:1}
.lb-sub{font-size:12px;color:var(--txt2);margin-top:4px;position:relative;z-index:1}
.lb-list-wrap{padding:0 16px 16px}
.lb-item{background:var(--card);border:1px solid var(--bdr2);border-radius:15px;padding:13px 15px;margin-bottom:8px;display:flex;align-items:center;gap:11px;transition:all .16s;box-shadow:var(--shadow)}
.lb-item.lb-me{border-color:var(--aborder);background:var(--adim)}
.lb-rank{font-family:'Syne',sans-serif;font-size:15px;font-weight:800;width:26px;text-align:center;flex-shrink:0}
.rank1{color:var(--yellow)}.rank2{color:#aaa}.rank3{color:#b87333}
.lb-av{width:40px;height:40px;border-radius:12px;display:flex;align-items:center;justify-content:center;font-size:15px;font-weight:700;font-family:'Syne',sans-serif;color:#fff;flex-shrink:0}
.lb-info{flex:1;min-width:0}
.lb-name{font-family:'Syne',sans-serif;font-size:13px;font-weight:700;color:var(--txt);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.lb-skill{font-size:11px;color:var(--txt3);margin-top:1px}
.lb-score{text-align:right;flex-shrink:0}
.lb-num{font-family:'Syne',sans-serif;font-size:16px;font-weight:800;color:var(--txt)}
.lb-numl{font-size:10px;color:var(--txt3)}

/* ═══════════════════════════════════════
   PROFILE
═══════════════════════════════════════ */
#sProfile{padding-bottom:var(--nav-h)}
.pr-scroll{flex:1;overflow-y:auto}
.pr-scroll::-webkit-scrollbar{display:none}
.pr-hero{padding:22px 18px 20px;background:var(--bg2);border-bottom:1px solid var(--bdr);text-align:center}
.pr-av-wrap{position:relative;display:inline-block;margin-bottom:13px}
.pr-av{width:78px;height:78px;border-radius:22px;background:var(--adim2);border:2px solid var(--aborder);display:flex;align-items:center;justify-content:center;font-size:29px;font-weight:700;font-family:'Syne',sans-serif;color:var(--accent)}
.pr-verified{position:absolute;bottom:-4px;right:-4px;width:22px;height:22px;background:var(--green);border-radius:50%;border:2px solid var(--bg);display:flex;align-items:center;justify-content:center}
.pr-name{font-family:'Syne',sans-serif;font-size:19px;font-weight:800;margin-bottom:3px;color:var(--txt)}
.pr-email{font-size:12px;color:var(--txt3);margin-bottom:14px}
.pr-stats{display:flex;gap:8px;justify-content:center;flex-wrap:wrap}
.pstat{background:var(--card);border:1px solid var(--bdr2);border-radius:12px;padding:9px 15px;text-align:center;box-shadow:var(--shadow)}
.pstat-n{font-family:'Syne',sans-serif;font-size:17px;font-weight:800;color:var(--txt)}
.pstat-l{font-size:10px;color:var(--txt3);text-transform:uppercase;letter-spacing:.5px;margin-top:1px}
.pr-body{padding:16px}
.skill-cols{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:14px}
.skill-col{background:var(--card);border:1px solid var(--bdr2);border-radius:15px;padding:13px;box-shadow:var(--shadow)}
.skill-col-title{font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:1px;margin-bottom:9px}
.skill-col-title.te{color:var(--accent)}.skill-col-title.le{color:var(--teal)}
.skill-item{font-size:12px;font-weight:600;color:var(--txt);margin-bottom:5px;display:flex;align-items:center;gap:6px}
.skill-dot{width:5px;height:5px;border-radius:50%;flex-shrink:0}
.dot-a{background:var(--accent)}.dot-t{background:var(--teal)}
.menu-list{background:var(--card);border:1px solid var(--bdr);border-radius:var(--r);overflow:hidden;margin-bottom:14px;box-shadow:var(--shadow)}
.menu-item{display:flex;align-items:center;gap:12px;padding:14px 16px;cursor:pointer;transition:background .14s;border-bottom:1px solid var(--bdr)}
.menu-item:last-child{border-bottom:none}
.menu-item:hover{background:var(--card2)}
.menu-ic{width:34px;height:34px;border-radius:10px;background:var(--surf);border:1px solid var(--bdr);display:flex;align-items:center;justify-content:center;flex-shrink:0}
.menu-label{flex:1;font-size:14px;font-weight:500;color:var(--txt)}
.menu-badge{background:var(--accent);color:#fff;font-size:10px;padding:2px 8px;border-radius:8px;font-weight:700;display:none}
.menu-chevron{color:var(--txt3)}

/* ═══════════════════════════════════════
   NOTIFICATIONS
═══════════════════════════════════════ */
#sNotif{overflow-y:auto}
.notif-scroll{flex:1;overflow-y:auto}
.notif-scroll::-webkit-scrollbar{display:none}
.notif-item{display:flex;gap:12px;align-items:flex-start;padding:14px 16px;border-bottom:1px solid var(--bdr);cursor:pointer;transition:background .14s}
.notif-item:hover{background:var(--card)}
.notif-item.unread{background:var(--adim)}
.notif-ic{width:38px;height:38px;border-radius:12px;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.notif-body{flex:1;min-width:0}
.notif-text{font-size:13px;color:var(--txt);line-height:1.5;margin-bottom:3px}
.notif-time{font-size:11px;color:var(--txt3)}
.notif-unread-dot{width:7px;height:7px;border-radius:50%;background:var(--accent);flex-shrink:0;margin-top:5px}

/* ═══════════════════════════════════════
   ONBOARDING
═══════════════════════════════════════ */
#sOnboard{overflow-y:auto;background:var(--bg)}
.onb-inner{overflow-y:auto;flex:1;padding:18px 18px 24px}
.step-bar{display:flex;gap:6px;margin-bottom:18px}
.step{flex:1;height:3px;border-radius:2px;background:var(--bdr2);transition:background .3s}
.step.done{background:var(--accent)}
.onb-title{font-family:'Syne',sans-serif;font-size:22px;font-weight:800;margin-bottom:6px;color:var(--txt)}
.onb-sub{font-size:13px;color:var(--txt2);line-height:1.6;margin-bottom:20px}

/* ═══════════════════════════════════════
   SETTINGS (inside Profile overlay)
═══════════════════════════════════════ */
#sSettings{overflow-y:auto;background:var(--bg)}
.settings-scroll{flex:1;overflow-y:auto}
.settings-scroll::-webkit-scrollbar{display:none}
.settings-sec{margin:0 16px 14px}
.settings-sec-title{font-size:11px;font-weight:700;color:var(--txt3);text-transform:uppercase;letter-spacing:1.2px;margin-bottom:8px;padding:0 4px}
.settings-list{background:var(--card);border:1px solid var(--bdr2);border-radius:var(--r);overflow:hidden;box-shadow:var(--shadow)}
.settings-item{display:flex;align-items:center;gap:12px;padding:14px 16px;border-bottom:1px solid var(--bdr);transition:background .14s}
.settings-item:last-child{border-bottom:none}
.settings-item:hover{background:var(--card2)}
.s-ic{width:34px;height:34px;border-radius:10px;background:var(--surf);border:1px solid var(--bdr);display:flex;align-items:center;justify-content:center;flex-shrink:0}
.s-label{flex:1;font-size:14px;font-weight:500;color:var(--txt)}
.s-value{font-size:12px;color:var(--txt3);font-weight:500}
/* Theme Toggle Switch */
.toggle-switch{position:relative;width:44px;height:24px;flex-shrink:0}
.toggle-switch input{opacity:0;width:0;height:0;position:absolute}
.toggle-track{position:absolute;inset:0;background:var(--bdr2);border-radius:12px;cursor:pointer;transition:background .22s}
.toggle-track::before{content:'';position:absolute;left:3px;top:3px;width:18px;height:18px;background:#fff;border-radius:50%;transition:transform .22s}
.toggle-switch input:checked + .toggle-track{background:var(--accent)}
.toggle-switch input:checked + .toggle-track::before{transform:translateX(20px)}

/* ═══════════════════════════════════════
   VIDEO CALL
═══════════════════════════════════════ */
#callScr{position:fixed;inset:0;z-index:500;background:#000;display:none;flex-direction:column}
#callScr.on{display:flex}
#remVid{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;background:#0a0a0e}
.loc-vid-wrap{position:absolute;bottom:100px;right:14px;width:106px;height:148px;border-radius:14px;overflow:hidden;background:var(--card3);border:2px solid rgba(255,255,255,0.14);z-index:10}
.loc-vid-wrap video{width:100%;height:100%;object-fit:cover}
.call-overlay{position:absolute;inset:0;z-index:11;display:flex;flex-direction:column;pointer-events:none}
.call-top{padding:48px 20px 14px;display:flex;align-items:flex-start;justify-content:space-between}
.call-name{font-family:'Syne',sans-serif;font-size:19px;font-weight:800;color:#fff;text-shadow:0 2px 8px rgba(0,0,0,0.8)}
.call-status{font-size:12px;color:rgba(255,255,255,0.6);margin-top:3px;text-shadow:0 1px 4px rgba(0,0,0,0.8)}
.call-timer{font-family:'Syne',sans-serif;font-size:13px;color:rgba(255,255,255,0.85);font-weight:700;background:rgba(0,0,0,0.42);padding:5px 12px;border-radius:20px;border:1px solid rgba(255,255,255,0.14)}
.call-controls{margin-top:auto;padding:18px 20px 48px;pointer-events:all}
.ctrl-row{display:flex;align-items:center;justify-content:center;gap:14px}
.ctrl-btn{width:56px;height:56px;border-radius:50%;display:flex;align-items:center;justify-content:center;transition:all .16s;cursor:pointer;border:none;outline:none}
.ctrl-mute,.ctrl-cam,.ctrl-spk{background:rgba(255,255,255,0.14);backdrop-filter:blur(8px)}
.ctrl-end{width:64px;height:64px;background:#e74c3c;box-shadow:0 4px 18px rgba(231,76,60,0.45)}
.ctrl-btn:hover{transform:scale(1.08)}
.ctrl-btn.off{background:rgba(255,92,53,0.28)}
.call-connecting{position:absolute;inset:0;background:rgba(0,0,0,0.88);z-index:15;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:16px}
.call-connecting.hidden{display:none}
.call-conn-spin{width:46px;height:46px;border:3px solid var(--bdr2);border-top-color:var(--accent);border-radius:50%;animation:spin 1s linear infinite}
.call-conn-title{font-family:'Syne',sans-serif;font-size:16px;font-weight:700;color:#fff}
.call-conn-name{font-size:13px;color:rgba(255,255,255,0.55)}

/* ═══════════════════════════════════════
   CHAT SCREEN
═══════════════════════════════════════ */
#sChat{background:var(--bg)}
.chat-topbar{display:flex;align-items:center;gap:11px;padding:0 14px;height:var(--topbar-h);border-bottom:1px solid var(--bdr);flex-shrink:0;background:var(--bg)}
.chat-back{width:34px;height:34px;display:flex;align-items:center;justify-content:center;cursor:pointer;flex-shrink:0}
.chat-av{width:36px;height:36px;border-radius:11px;display:flex;align-items:center;justify-content:center;font-size:13px;font-weight:700;font-family:'Syne',sans-serif;color:#fff;flex-shrink:0}
.chat-info{flex:1;min-width:0}
.chat-name{font-family:'Syne',sans-serif;font-size:14px;font-weight:800;color:var(--txt);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.chat-sub{font-size:11px;color:var(--teal);font-weight:600}
.chat-msgs{flex:1;overflow-y:auto;padding:14px 14px 8px;display:flex;flex-direction:column;gap:8px}
.chat-msgs::-webkit-scrollbar{display:none}
.chat-bubble-wrap{display:flex;flex-direction:column;align-items:flex-start;max-width:80%}
.chat-bubble-wrap.mine{align-self:flex-end;align-items:flex-end}
.chat-bubble{padding:10px 13px;border-radius:18px;font-size:13.5px;line-height:1.5;word-break:break-word;color:var(--txt)}
.chat-bubble-wrap.mine .chat-bubble{background:var(--accent);color:#fff;border-bottom-right-radius:5px}
.chat-bubble-wrap:not(.mine) .chat-bubble{background:var(--card2);border:1px solid var(--bdr2);border-bottom-left-radius:5px}
.chat-time{font-size:10px;color:var(--txt3);margin-top:3px;padding:0 4px}
.chat-date-sep{align-self:center;font-size:10.5px;color:var(--txt3);background:var(--card);border:1px solid var(--bdr2);border-radius:20px;padding:3px 12px;font-weight:600;margin:4px 0}
.chat-input-bar{display:flex;align-items:flex-end;gap:9px;padding:10px 14px 14px;border-top:1px solid var(--bdr);flex-shrink:0;background:var(--bg)}
.chat-inp{flex:1;background:var(--card);border:1.5px solid var(--bdr2);border-radius:20px;padding:10px 15px;font-size:14px;color:var(--txt);outline:none;resize:none;max-height:100px;min-height:42px;transition:border-color .18s;line-height:1.4;font-family:'DM Sans',sans-serif}
.chat-inp:focus{border-color:var(--accent)}
.chat-inp::placeholder{color:var(--txt3)}
.chat-send{width:42px;height:42px;border-radius:50%;background:var(--accent);display:flex;align-items:center;justify-content:center;cursor:pointer;flex-shrink:0;border:none;transition:all .16s;box-shadow:0 3px 12px rgba(255,92,53,.35)}
.chat-send:hover{transform:scale(1.08);box-shadow:0 5px 18px rgba(255,92,53,.5)}
.chat-send:active{transform:scale(0.96)}
.chat-send:disabled{opacity:0.4;cursor:default;transform:none}
.chat-empty{flex:1;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:10px;padding:30px}
.chat-empty-icon{width:54px;height:54px;background:var(--adim);border:1px solid var(--aborder);border-radius:18px;display:flex;align-items:center;justify-content:center}
.chat-empty-txt{font-size:13px;color:var(--txt3);text-align:center;line-height:1.55}

/* ═══════════════════════════════════════
   BOTTOM SHEETS / OVERLAYS
═══════════════════════════════════════ */
.overlay{display:none;position:fixed;inset:0;background:rgba(0,0,0,0.72);z-index:300;align-items:flex-end;justify-content:center;backdrop-filter:blur(5px)}
.overlay.on{display:flex;animation:fadeOv .2s}
@keyframes fadeOv{from{opacity:0}to{opacity:1}}
.sheet{background:var(--card);border-top:1px solid var(--bdr2);border-radius:22px 22px 0 0;padding:20px 20px 38px;width:100%;max-width:430px;animation:sheetUp .28s cubic-bezier(0.22,1,0.36,1);max-height:88vh;overflow-y:auto}
@keyframes sheetUp{from{transform:translateY(100%)}to{transform:translateY(0)}}
.sheet-handle{width:36px;height:3px;background:var(--bdr2);border-radius:2px;margin:0 auto 18px}
.sheet-title{font-family:'Syne',sans-serif;font-size:18px;font-weight:800;margin-bottom:5px;color:var(--txt)}
.sheet-sub{font-size:13px;color:var(--txt2);margin-bottom:18px;line-height:1.5}
.sheet-row{display:flex;justify-content:space-between;align-items:center;padding:11px 0;border-top:1px solid var(--bdr);font-size:13px}
.sheet-key{color:var(--txt2);display:flex;align-items:center;gap:6px}
.sheet-val{font-weight:700;color:var(--txt)}
.sheet-val.a{color:var(--accent)}.sheet-val.t{color:var(--teal)}.sheet-val.g{color:var(--green)}
.session-grid{display:grid;grid-template-columns:1fr 1fr;gap:9px;margin-bottom:18px}
.sess-option{background:var(--card2);border:1.5px solid var(--bdr2);border-radius:15px;padding:14px 12px;text-align:center;cursor:pointer;transition:all .16s}
.sess-option.on{border-color:var(--accent);background:var(--adim)}
.sess-icon{margin-bottom:7px;display:flex;justify-content:center}
.sess-title{font-family:'Syne',sans-serif;font-size:13px;font-weight:700;color:var(--txt);margin-bottom:2px}
.sess-sub{font-size:11px;color:var(--txt3)}
.report-option{background:var(--card2);border:1px solid var(--bdr2);border-radius:12px;padding:12px 14px;margin-bottom:7px;cursor:pointer;transition:all .16s;display:flex;align-items:center;gap:10px}
.report-option:hover{border-color:var(--aborder)}
.report-option.on{border-color:var(--accent);background:var(--adim)}
.report-text{font-size:13px;color:var(--txt);font-weight:500}
.textarea-inp{width:100%;background:var(--surf);border:1.5px solid var(--bdr2);border-radius:12px;padding:11px 13px;font-size:13px;color:var(--txt);resize:none;height:84px;outline:none;margin-top:7px;transition:border-color .18s;-webkit-appearance:none}
.textarea-inp:focus{border-color:var(--accent)}
.textarea-inp::placeholder{color:var(--txt3)}
.star-row{display:flex;justify-content:center;gap:10px;margin:14px 0}
.star-btn{width:44px;height:44px;background:var(--card2);border:1px solid var(--bdr2);border-radius:13px;display:flex;align-items:center;justify-content:center;cursor:pointer;transition:all .16s}
.star-btn.on{background:rgba(240,180,41,0.14);border-color:rgba(240,180,41,0.3)}

/* ═══════════════════════════════════════
   TOAST
═══════════════════════════════════════ */
#toast{position:fixed;bottom:calc(var(--nav-h) + 12px);left:50%;transform:translateX(-50%) translateY(10px);background:var(--card2);color:var(--txt);padding:10px 20px;border-radius:22px;font-size:13px;font-weight:500;opacity:0;transition:all .26s;z-index:1000;white-space:nowrap;pointer-events:none;max-width:300px;text-align:center;border:1px solid var(--bdr2);box-shadow:var(--shadow2)}
#toast.show{opacity:1;transform:translateX(-50%) translateY(0)}
#toast.ok{border-color:rgba(39,174,96,0.3);color:var(--green)}
#toast.er{border-color:var(--aborder);color:var(--accent)}

/* ═══════════════════════════════════════
   LOADER
═══════════════════════════════════════ */
.loader{display:flex;align-items:center;justify-content:center;gap:10px;padding:40px 20px;color:var(--txt3);font-size:13px}
.spin-ring{width:18px;height:18px;border:2px solid var(--bdr2);border-top-color:var(--accent);border-radius:50%;animation:spin .8s linear infinite;flex-shrink:0}
.error-state{text-align:center;padding:40px 20px;color:var(--txt2);font-size:13px;line-height:1.6}

/* ═══════════════════════════════════════
   SVG ICON HELPERS
═══════════════════════════════════════ */
svg{display:block;flex-shrink:0}
</style>
</head>
<body>
<!-- Instant splash: pure HTML/CSS, shows before any JS -->
<div id="instantSplash">
  <div class="is-orbs">
    <div class="is-o1 is-orb"><svg width="21" height="21" viewBox="0 0 24 24" fill="none" stroke="#ff5c35" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M9 18V5l12-2v13"/><circle cx="6" cy="18" r="3"/><circle cx="18" cy="16" r="3"/></svg></div>
    <div class="is-o2 is-orb"><svg width="29" height="29" viewBox="0 0 24 24" fill="none" stroke="#505060" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><polyline points="17 1 21 5 17 9"/><path d="M3 11V9a4 4 0 014-4h14"/><polyline points="7 23 3 19 7 15"/><path d="M21 13v2a4 4 0 01-4 4H3"/></svg></div>
    <div class="is-o3 is-orb"><svg width="21" height="21" viewBox="0 0 24 24" fill="none" stroke="#00c9a7" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg></div>
  </div>
  <div class="is-brand">Swap<span>Skill</span></div>
  <div class="is-tag">Your skill is someone's dream</div>
  <div class="is-ring"></div>
</div>
<div id="app">
<div id="toast"></div>

<!-- ══════════════════════════ SPLASH ══════════════════════════ -->
<div class="scr" id="sSplash">
  <div class="splash-inner" style="margin:auto">
    <div class="splash-orbs">
      <div class="orb orb1">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M9 18V5l12-2v13"/><circle cx="6" cy="18" r="3"/><circle cx="18" cy="16" r="3"/></svg>
      </div>
      <div class="orb orb2">
        <svg width="30" height="30" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><polyline points="17 1 21 5 17 9"/><path d="M3 11V9a4 4 0 014-4h14"/><polyline points="7 23 3 19 7 15"/><path d="M21 13v2a4 4 0 01-4 4H3"/></svg>
      </div>
      <div class="orb orb3">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="var(--teal)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
      </div>
    </div>
    <div class="brand">Swap<span>Skill</span></div>
    <div class="brand-tag">Your skill is someone's dream</div>
    <div class="splash-ring"></div>
  </div>
</div>

<!-- ══════════════════════════ AUTH ══════════════════════════ -->
<div class="scr" id="sAuth">
  <div class="auth-glow-a"></div><div class="auth-glow-b"></div>
  <div class="auth-body">
    <div class="auth-logo-area">
      <div class="auth-orbs">
        <div class="orb orb1" style="width:48px;height:48px">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M9 18V5l12-2v13"/><circle cx="6" cy="18" r="3"/><circle cx="18" cy="16" r="3"/></svg>
        </div>
        <div class="orb orb2" style="width:62px;height:62px">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><polyline points="17 1 21 5 17 9"/><path d="M3 11V9a4 4 0 014-4h14"/><polyline points="7 23 3 19 7 15"/><path d="M21 13v2a4 4 0 01-4 4H3"/></svg>
        </div>
        <div class="orb orb3" style="width:48px;height:48px">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="var(--teal)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
        </div>
      </div>
      <div class="brand" style="font-size:38px">Swap<span>Skill</span></div>
      <div class="brand-tag">Your skill is someone's dream</div>
    </div>
    <div class="auth-card">
      <div class="tab-row">
        <div class="auth-tab on" id="tabLogin" onclick="authTab('login')">Sign In</div>
        <div class="auth-tab" id="tabSignup" onclick="authTab('signup')">Create Account</div>
      </div>
      <div class="auth-err" id="authErr"></div>
      <!-- Login form -->
      <div class="aform on" id="fLogin">
        <div class="field"><div class="field-lbl">Email</div><input class="inp" type="email" id="lEmail" placeholder="you@email.com" autocomplete="email"></div>
        <div class="field" style="margin-bottom:18px"><div class="field-lbl">Password</div><input class="inp" type="password" id="lPass" placeholder="Enter password" autocomplete="current-password"></div>
        <button class="btn btn-primary" id="btnLogin" onclick="doLogin()"><span id="btnLoginTxt">Sign In</span></button>
      </div>
      <!-- Signup form -->
      <div class="aform" id="fSignup">
        <div class="field"><div class="field-lbl">Full Name</div><input class="inp" type="text" id="sName" placeholder="Your full name" autocomplete="name"></div>
        <div class="field"><div class="field-lbl">Email</div><input class="inp" type="email" id="sEmail" placeholder="you@email.com" autocomplete="email"></div>
        <div class="field" style="margin-bottom:18px"><div class="field-lbl">Password</div><input class="inp" type="password" id="sPass" placeholder="Min 6 characters" autocomplete="new-password"></div>
        <button class="btn btn-primary" id="btnSignup" onclick="doSignup()"><span id="btnSignupTxt">Create Account</span></button>
      </div>

    </div>
    <div style="font-size:11px;color:var(--txt3);text-align:center;margin-top:16px;line-height:1.7">Free forever &nbsp;·&nbsp; No payments &nbsp;·&nbsp; Just skills</div>
  </div>
</div>

<!-- ══════════════════════════ ONBOARDING ══════════════════════════ -->
<div class="scr" id="sOnboard">
  <div class="topbar"><div class="t-brand">Swap<span>Skill</span></div><div style="font-size:12px;color:var(--txt3)" id="onbStep">Step 1 of 2</div></div>
  <div class="onb-inner">
    <div class="step-bar"><div class="step done" id="os1"></div><div class="step" id="os2"></div></div>
    <div id="onbP1">
      <div class="onb-title">What can you teach?</div>
      <div class="onb-sub">Pick up to 3 skills you know well and enjoy sharing with others.</div>
      <input class="pill-search" id="onbTeachQ" placeholder="Search skills..." oninput="filterPills('onbTeachPills',this.value,'p-teach')">
      <div class="sel-count" id="onbTCnt">0 / 3 selected</div>
      <div class="pills-wrap" id="onbTeachPills"></div>
      <br>
      <button class="btn btn-primary" onclick="onbNext()" style="margin-top:8px">Continue <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg></button>
    </div>
    <div id="onbP2" style="display:none">
      <div class="onb-title">What do you want to learn?</div>
      <div class="onb-sub">Pick up to 3 skills. These power your matches.</div>
      <input class="pill-search" id="onbLearnQ" placeholder="Search skills..." oninput="filterPills('onbLearnPills',this.value,'p-learn')">
      <div class="sel-count" id="onbLCnt">0 / 3 selected</div>
      <div class="pills-wrap" id="onbLearnPills"></div>
      <br>
      <button class="btn btn-primary" onclick="onbFinish()" style="margin-top:8px"><span id="onbFinTxt">Finish &amp; Find Matches</span></button>
      <button class="btn btn-ghost" onclick="onbBack()">Back</button>
    </div>
  </div>
</div>

<!-- ══════════════════════════ HOME ══════════════════════════ -->
<div class="scr" id="sHome">
  <div class="topbar">
    <div class="t-brand">Swap<span>Skill</span></div>
    <div class="t-right">
      <div class="icon-btn" onclick="navTo('sNotif',loadNotifs)" title="Notifications">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="var(--txt2)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M18 8A6 6 0 006 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 01-3.46 0"/></svg>
        <div class="notif-dot" id="notifDot"></div>
      </div>
    </div>
  </div>
  <div class="home-scroll">
    <div class="home-hero">
      <div class="hero-greeting">Welcome back</div>
      <div class="hero-name" id="heroName">Find your perfect <span>Skill Swap</span></div>
      <div class="skill-chips" id="heroChips"></div>
    </div>
    <div class="pad">
      <div class="qa-grid" style="margin-top:4px">
        <div class="qa-card qa-teach" onclick="navTo('sSetup',null)">
          <div class="qa-ic"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="rgba(255,255,255,.9)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M4 19.5A2.5 2.5 0 016.5 17H20"/><path d="M6.5 2H20v20H6.5A2.5 2.5 0 014 19.5v-15A2.5 2.5 0 016.5 2z"/></svg></div>
          <div class="qa-title">I Can Teach</div><div class="qa-sub">Update my skills</div>
          <div class="qa-arr"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="rgba(255,255,255,.55)" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg></div>
        </div>
        <div class="qa-card qa-learn" onclick="navTo('sExplore',loadExplore)">
          <div class="qa-ic"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="rgba(255,255,255,.9)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg></div>
          <div class="qa-title">I Want to Learn</div><div class="qa-sub">Find a teacher</div>
          <div class="qa-arr"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="rgba(255,255,255,.55)" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg></div>
        </div>
      </div>
      <div class="sec-head"><div class="sec-title">Top Matches For You</div><div class="sec-link" onclick="navTo('sExplore',loadExplore)">See all</div></div>
      <div class="match-scroll" id="homeMatches"><div class="loader"><div class="spin-ring"></div> Loading…</div></div>
      <div class="sec-head"><div class="sec-title">Community Stats</div></div>
      <div class="stats-row">
        <div><div class="s-num" id="statUsers">—</div><div class="s-lbl">Learners</div></div>
        <div class="s-divider"></div>
        <div><div class="s-num" id="statSwaps">—</div><div class="s-lbl">Swaps Done</div></div>
        <div class="s-divider"></div>
        <div><div class="s-num">80+</div><div class="s-lbl">Skills</div></div>
      </div>
    </div>
  </div>
  <nav class="bnav" id="bnavHome">
    <div class="ni on" onclick="navTo('sHome',null)"><div class="ni-ic"><svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 9l9-7 9 7v11a2 2 0 01-2 2H5a2 2 0 01-2-2V9z"/></svg></div><div class="ni-lbl" style="color:var(--accent)">Home</div></div>
    <div class="ni" onclick="navTo('sExplore',loadExplore)"><div class="ni-ic"><svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg></div><div class="ni-lbl">Explore</div></div>
    <div class="ni-fab" onclick="navTo('sSetup',null)"><div class="ni-fab-btn"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg></div><div class="ni-fab-lbl">Post</div></div>
    <div class="ni" onclick="navTo('sSwaps',loadSwaps)"><div class="ni-ic"><svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="17 1 21 5 17 9"/><path d="M3 11V9a4 4 0 014-4h14"/><polyline points="7 23 3 19 7 15"/><path d="M21 13v2a4 4 0 01-4 4H3"/></svg></div><div class="ni-lbl">Swaps</div></div>
    <div class="ni" onclick="navTo('sProfile',refreshProfile)"><div class="ni-ic"><svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 21v-2a4 4 0 00-4-4H8a4 4 0 00-4 4v2"/><circle cx="12" cy="7" r="4"/></svg></div><div class="ni-lbl">Profile</div></div>
  </nav>
</div>

<!-- ══════════════════════════ EXPLORE ══════════════════════════ -->
<div class="scr" id="sExplore">
  <div class="topbar"><div class="t-brand">Swap<span>Skill</span></div></div>
  <div class="search-bar-wrap">
    <div class="search-bar">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
      <input type="search" id="searchInp" placeholder="Search skills, people…" oninput="renderCards()">
    </div>
  </div>
  <div class="filter-chips" id="filterChips">
    <div class="fchip on" onclick="setFilter(this,'All')"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>All</div>
    <div class="fchip" onclick="setFilter(this,'Tech')"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="3" width="20" height="14" rx="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/></svg>Tech</div>
    <div class="fchip" onclick="setFilter(this,'Music')"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 18V5l12-2v13"/><circle cx="6" cy="18" r="3"/><circle cx="18" cy="16" r="3"/></svg>Music</div>
    <div class="fchip" onclick="setFilter(this,'Cooking')"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 2v7c0 1.1.9 2 2 2h4a2 2 0 002-2V2"/><path d="M7 2v20"/><path d="M21 15V2a5 5 0 00-5 5v6c0 1.1.9 2 2 2h3zm0 0v7"/></svg>Cooking</div>
    <div class="fchip" onclick="setFilter(this,'Language')"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/><path d="M12 2a15.3 15.3 0 014 10 15.3 15.3 0 01-4 10 15.3 15.3 0 01-4-10 15.3 15.3 0 014-10z"/></svg>Language</div>
    <div class="fchip" onclick="setFilter(this,'Art')"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="13.5" cy="6.5" r=".5"/><circle cx="17.5" cy="10.5" r=".5"/><circle cx="8.5" cy="7.5" r=".5"/><circle cx="6.5" cy="12.5" r=".5"/><path d="M12 2C6.5 2 2 6.5 2 12s4.5 10 10 10c.926 0 1.648-.746 1.648-1.688 0-.437-.18-.835-.437-1.125-.29-.289-.438-.652-.438-1.125a1.64 1.64 0 011.668-1.668h1.996c3.051 0 5.555-2.503 5.555-5.554C21.965 6.012 17.461 2 12 2z"/></svg>Art</div>
    <div class="fchip" onclick="setFilter(this,'Finance')"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 000 7h5a3.5 3.5 0 010 7H6"/></svg>Finance</div>
    <div class="fchip" onclick="setFilter(this,'Fitness')"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 8h1a4 4 0 010 8h-1"/><path d="M2 8h16v9a4 4 0 01-4 4H6a4 4 0 01-4-4V8z"/><line x1="6" y1="1" x2="6" y2="4"/><line x1="10" y1="1" x2="10" y2="4"/><line x1="14" y1="1" x2="14" y2="4"/></svg>Fitness</div>
    <div class="fchip" onclick="setFilter(this,'Academic')"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 19.5A2.5 2.5 0 016.5 17H20"/><path d="M6.5 2H20v20H6.5A2.5 2.5 0 014 19.5v-15A2.5 2.5 0 016.5 2z"/></svg>Academic</div>
  </div>
  <div class="exp-scroll"><div class="exp-list" id="expList"><div class="loader"><div class="spin-ring"></div> Loading people…</div></div></div>
  <nav class="bnav">
    <div class="ni" onclick="navTo('sHome',null)"><div class="ni-ic"><svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 9l9-7 9 7v11a2 2 0 01-2 2H5a2 2 0 01-2-2V9z"/></svg></div><div class="ni-lbl">Home</div></div>
    <div class="ni on"><div class="ni-ic"><svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg></div><div class="ni-lbl" style="color:var(--accent)">Explore</div></div>
    <div class="ni-fab" onclick="navTo('sSetup',null)"><div class="ni-fab-btn"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg></div><div class="ni-fab-lbl">Post</div></div>
    <div class="ni" onclick="navTo('sSwaps',loadSwaps)"><div class="ni-ic"><svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="17 1 21 5 17 9"/><path d="M3 11V9a4 4 0 014-4h14"/><polyline points="7 23 3 19 7 15"/><path d="M21 13v2a4 4 0 01-4 4H3"/></svg></div><div class="ni-lbl">Swaps</div></div>
    <div class="ni" onclick="navTo('sProfile',refreshProfile)"><div class="ni-ic"><svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 21v-2a4 4 0 00-4-4H8a4 4 0 00-4 4v2"/><circle cx="12" cy="7" r="4"/></svg></div><div class="ni-lbl">Profile</div></div>
  </nav>
</div>

<!-- ══════════════════════════ DETAIL ══════════════════════════ -->
<div class="scr" id="sDetail">
  <div class="back-bar" onclick="goBack()">
    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="var(--txt2)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"/></svg>
    <span>Back</span>
  </div>
  <div class="det-inner">
    <div class="det-hero">
      <div class="det-av" id="detAv"></div>
      <div class="det-name" id="detName"></div>
      <div class="det-badges" id="detBadges"></div>
      <div class="det-stats">
        <div class="det-stat"><div class="det-stat-n" id="detRating">—</div><div class="det-stat-l">Rating</div></div>
        <div class="det-stat" style="margin:0 18px"><div class="det-stat-n" id="detSwaps">—</div><div class="det-stat-l">Swaps</div></div>
        <div class="det-stat"><div class="det-stat-n" id="detYear">—</div><div class="det-stat-l">Member</div></div>
      </div>
    </div>
    <div class="det-body">
      <div class="det-box">
        <div class="det-box-title">Skill Exchange</div>
        <div class="det-swap-grid">
          <div class="det-swap-cell"><div class="det-cell-lbl" style="color:var(--accent)">They Teach</div><div class="det-cell-val" id="detTeach"></div></div>
          <div class="swap-ic"><svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="17 1 21 5 17 9"/><path d="M3 11V9a4 4 0 014-4h14"/><polyline points="7 23 3 19 7 15"/><path d="M21 13v2a4 4 0 01-4 4H3"/></svg></div>
          <div class="det-swap-cell"><div class="det-cell-lbl" style="color:var(--teal)">They Learn</div><div class="det-cell-val" id="detLearn"></div></div>
        </div>
      </div>
      <div class="det-box"><div class="det-bio" id="detBio">No bio yet.</div></div>
      <button class="btn btn-primary" onclick="openSwapRequest()">
        <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="17 1 21 5 17 9"/><path d="M3 11V9a4 4 0 014-4h14"/><polyline points="7 23 3 19 7 15"/><path d="M21 13v2a4 4 0 01-4 4H3"/></svg>
        Send Swap Request
      </button>
      <button class="btn btn-ghost" onclick="openReport()" style="margin-top:9px">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>
        Report User
      </button>
    </div>
  </div>
</div>

<!-- ══════════════════════════ MY SWAPS ══════════════════════════ -->
<div class="scr" id="sSwaps">
  <div class="topbar"><div class="t-brand">My <span>Swaps</span></div></div>
  <div class="tabs-bar">
    <div class="sw-tab on" onclick="swapsTab(this,'received')">Received</div>
    <div class="sw-tab" onclick="swapsTab(this,'sent')">Sent</div>
    <div class="sw-tab" onclick="swapsTab(this,'active')">Active</div>
    <div class="sw-tab" onclick="swapsTab(this,'done')">Done</div>
  </div>
  <div class="sw-scroll"><div class="sw-list" id="swapsList"><div class="loader"><div class="spin-ring"></div> Loading…</div></div></div>
  <nav class="bnav">
    <div class="ni" onclick="navTo('sHome',null)"><div class="ni-ic"><svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 9l9-7 9 7v11a2 2 0 01-2 2H5a2 2 0 01-2-2V9z"/></svg></div><div class="ni-lbl">Home</div></div>
    <div class="ni" onclick="navTo('sExplore',loadExplore)"><div class="ni-ic"><svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg></div><div class="ni-lbl">Explore</div></div>
    <div class="ni-fab" onclick="navTo('sSetup',null)"><div class="ni-fab-btn"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg></div><div class="ni-fab-lbl">Post</div></div>
    <div class="ni on"><div class="ni-ic"><svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="17 1 21 5 17 9"/><path d="M3 11V9a4 4 0 014-4h14"/><polyline points="7 23 3 19 7 15"/><path d="M21 13v2a4 4 0 01-4 4H3"/></svg></div><div class="ni-lbl" style="color:var(--accent)">Swaps</div></div>
    <div class="ni" onclick="navTo('sProfile',refreshProfile)"><div class="ni-ic"><svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 21v-2a4 4 0 00-4-4H8a4 4 0 00-4 4v2"/><circle cx="12" cy="7" r="4"/></svg></div><div class="ni-lbl">Profile</div></div>
  </nav>
</div>

<!-- ══════════════════════════ POST / SETUP ══════════════════════════ -->
<div class="scr" id="sSetup">
  <div class="topbar"><div class="t-brand">Update <span>Skills</span></div></div>
  <div class="setup-scroll">
    <div style="height:14px"></div>
    <div class="setup-sec">
      <div class="setup-sec-title"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 19.5A2.5 2.5 0 016.5 17H20"/><path d="M6.5 2H20v20H6.5A2.5 2.5 0 014 19.5v-15A2.5 2.5 0 016.5 2z"/></svg> I Can Teach (max 3)</div>
      <input class="pill-search" id="setupTeachQ" placeholder="Search skills…" oninput="filterPills('setupTeachPills',this.value,'p-teach')">
      <div class="sel-count" id="stpTCnt">0 / 3</div>
      <div class="pills-wrap" id="setupTeachPills"></div>
    </div>
    <div class="setup-sec">
      <div class="setup-sec-title"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--teal)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg> I Want to Learn (max 3)</div>
      <input class="pill-search" id="setupLearnQ" placeholder="Search skills…" oninput="filterPills('setupLearnPills',this.value,'p-learn')">
      <div class="sel-count" id="stpLCnt">0 / 3</div>
      <div class="pills-wrap" id="setupLearnPills"></div>
    </div>
    <div class="setup-sec">
      <div class="setup-sec-title"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--txt2)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 21v-2a4 4 0 00-4-4H8a4 4 0 00-4 4v2"/><circle cx="12" cy="7" r="4"/></svg> About Me</div>
      <textarea class="textarea-inp" id="setupBio" placeholder="Tell others about yourself, your experience, availability…" style="margin-top:0"></textarea>
    </div>
    <div style="padding:0 16px 24px"><button class="btn btn-primary" onclick="saveProfile()"><span id="saveTxt">Save &amp; Go Live</span></button></div>
  </div>
  <nav class="bnav">
    <div class="ni" onclick="navTo('sHome',null)"><div class="ni-ic"><svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 9l9-7 9 7v11a2 2 0 01-2 2H5a2 2 0 01-2-2V9z"/></svg></div><div class="ni-lbl">Home</div></div>
    <div class="ni" onclick="navTo('sExplore',loadExplore)"><div class="ni-ic"><svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg></div><div class="ni-lbl">Explore</div></div>
    <div class="ni-fab on" onclick="navTo('sSetup',null)"><div class="ni-fab-btn" style="box-shadow:0 6px 20px rgba(255,92,53,.6)"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg></div><div class="ni-fab-lbl" style="color:var(--accent)">Post</div></div>
    <div class="ni" onclick="navTo('sSwaps',loadSwaps)"><div class="ni-ic"><svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="17 1 21 5 17 9"/><path d="M3 11V9a4 4 0 014-4h14"/><polyline points="7 23 3 19 7 15"/><path d="M21 13v2a4 4 0 01-4 4H3"/></svg></div><div class="ni-lbl">Swaps</div></div>
    <div class="ni" onclick="navTo('sProfile',refreshProfile)"><div class="ni-ic"><svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 21v-2a4 4 0 00-4-4H8a4 4 0 00-4 4v2"/><circle cx="12" cy="7" r="4"/></svg></div><div class="ni-lbl">Profile</div></div>
  </nav>
</div>

<!-- ══════════════════════════ LEADERBOARD ══════════════════════════ -->
<div class="scr" id="sLboard">
  <div class="topbar"><div class="t-brand">Leader<span>board</span></div></div>
  <div class="lb-scroll">
    <div class="lb-hero">
      <div style="display:flex;align-items:center;justify-content:center;margin-bottom:8px">
        <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="var(--yellow)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
      </div>
      <div class="lb-title">Top Skill Swappers</div>
      <div class="lb-sub">Ranked by completed swaps</div>
    </div>
    <div class="lb-list-wrap" id="lbList"><div class="loader"><div class="spin-ring"></div> Loading…</div></div>
  </div>
  <nav class="bnav">
    <div class="ni" onclick="navTo('sHome',null)"><div class="ni-ic"><svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 9l9-7 9 7v11a2 2 0 01-2 2H5a2 2 0 01-2-2V9z"/></svg></div><div class="ni-lbl">Home</div></div>
    <div class="ni" onclick="navTo('sExplore',loadExplore)"><div class="ni-ic"><svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg></div><div class="ni-lbl">Explore</div></div>
    <div class="ni-fab" onclick="navTo('sSetup',null)"><div class="ni-fab-btn"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg></div><div class="ni-fab-lbl">Post</div></div>
    <div class="ni" onclick="navTo('sSwaps',loadSwaps)"><div class="ni-ic"><svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="17 1 21 5 17 9"/><path d="M3 11V9a4 4 0 014-4h14"/><polyline points="7 23 3 19 7 15"/><path d="M21 13v2a4 4 0 01-4 4H3"/></svg></div><div class="ni-lbl">Swaps</div></div>
    <div class="ni on" onclick="navTo('sProfile',refreshProfile)"><div class="ni-ic"><svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 21v-2a4 4 0 00-4-4H8a4 4 0 00-4 4v2"/><circle cx="12" cy="7" r="4"/></svg></div><div class="ni-lbl" style="color:var(--accent)">Profile</div></div>
  </nav>
</div>

<!-- ══════════════════════════ PROFILE ══════════════════════════ -->
<div class="scr" id="sProfile">
  <div class="topbar"><div class="t-brand">My <span>Profile</span></div></div>
  <div class="pr-scroll">
    <div class="pr-hero">
      <div class="pr-av-wrap">
        <div class="pr-av" id="prAv">?</div>
        <div class="pr-verified"><svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></div>
      </div>
      <div class="pr-name" id="prName">—</div>
      <div class="pr-email" id="prEmail">—</div>
      <div class="pr-stats">
        <div class="pstat"><div class="pstat-n" id="prSwaps">0</div><div class="pstat-l">Swaps</div></div>
        <div class="pstat"><div class="pstat-n" id="prRating">—</div><div class="pstat-l">Rating</div></div>
        <div class="pstat"><div class="pstat-n" id="prStreak">0</div><div class="pstat-l">Streak</div></div>
      </div>
    </div>
    <div class="pr-body">
      <div class="skill-cols">
        <div class="skill-col"><div class="skill-col-title te">I Teach</div><div id="prTeach"><div style="font-size:12px;color:var(--txt3)">None set</div></div></div>
        <div class="skill-col"><div class="skill-col-title le">I Learn</div><div id="prLearn"><div style="font-size:12px;color:var(--txt3)">None set</div></div></div>
      </div>
      <div class="menu-list">
        <div class="menu-item" onclick="navTo('sSetup',null)">
          <div class="menu-ic"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M11 4H4a2 2 0 00-2 2v14a2 2 0 002 2h14a2 2 0 002-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 013 3L12 15l-4 1 1-4 9.5-9.5z"/></svg></div>
          <div class="menu-label">Edit My Skills</div>
          <svg class="menu-chevron" width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>
        </div>
        <div class="menu-item" onclick="navTo('sLboard',loadLboard)">
          <div class="menu-ic"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--yellow)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg></div>
          <div class="menu-label">Leaderboard</div>
          <svg class="menu-chevron" width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>
        </div>
        <div class="menu-item" onclick="navTo('sSwaps',loadSwaps)">
          <div class="menu-ic"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--teal)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><polyline points="17 1 21 5 17 9"/><path d="M3 11V9a4 4 0 014-4h14"/><polyline points="7 23 3 19 7 15"/><path d="M21 13v2a4 4 0 01-4 4H3"/></svg></div>
          <div class="menu-label">My Swap History</div>
          <div class="menu-badge" id="pendingBadge">0</div>
          <svg class="menu-chevron" width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>
        </div>
        <div class="menu-item" onclick="navTo('sNotif',loadNotifs)">
          <div class="menu-ic"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--txt2)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M18 8A6 6 0 006 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 01-3.46 0"/></svg></div>
          <div class="menu-label">Notifications</div>
          <svg class="menu-chevron" width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>
        </div>
        <div class="menu-item" onclick="navTo('sSettings',null)">
          <div class="menu-ic"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--blue)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 00.33 1.82l.06.06a2 2 0 010 2.83 2 2 0 01-2.83 0l-.06-.06a1.65 1.65 0 00-1.82-.33 1.65 1.65 0 00-1 1.51V21a2 2 0 01-2 2 2 2 0 01-2-2v-.09A1.65 1.65 0 009 19.4a1.65 1.65 0 00-1.82.33l-.06.06a2 2 0 01-2.83 0 2 2 0 010-2.83l.06-.06A1.65 1.65 0 004.68 15a1.65 1.65 0 00-1.51-1H3a2 2 0 01-2-2 2 2 0 012-2h.09A1.65 1.65 0 004.6 9a1.65 1.65 0 00-.33-1.82l-.06-.06a2 2 0 010-2.83 2 2 0 012.83 0l.06.06A1.65 1.65 0 009 4.68a1.65 1.65 0 001-1.51V3a2 2 0 012-2 2 2 0 012 2v.09a1.65 1.65 0 001 1.51 1.65 1.65 0 001.82-.33l.06-.06a2 2 0 012.83 0 2 2 0 010 2.83l-.06.06A1.65 1.65 0 0019.4 9a1.65 1.65 0 001.51 1H21a2 2 0 012 2 2 2 0 01-2 2h-.09a1.65 1.65 0 00-1.51 1z"/></svg></div>
          <div class="menu-label">Settings</div>
          <svg class="menu-chevron" width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>
        </div>
        <div class="menu-item" onclick="doLogout()">
          <div class="menu-ic"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M9 21H5a2 2 0 01-2-2V5a2 2 0 012-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" y1="12" x2="9" y2="12"/></svg></div>
          <div class="menu-label">Log Out</div>
          <svg class="menu-chevron" width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>
        </div>
      </div>
    </div>
  </div>
  <nav class="bnav">
    <div class="ni" onclick="navTo('sHome',null)"><div class="ni-ic"><svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 9l9-7 9 7v11a2 2 0 01-2 2H5a2 2 0 01-2-2V9z"/></svg></div><div class="ni-lbl">Home</div></div>
    <div class="ni" onclick="navTo('sExplore',loadExplore)"><div class="ni-ic"><svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg></div><div class="ni-lbl">Explore</div></div>
    <div class="ni-fab" onclick="navTo('sSetup',null)"><div class="ni-fab-btn"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg></div><div class="ni-fab-lbl">Post</div></div>
    <div class="ni" onclick="navTo('sSwaps',loadSwaps)"><div class="ni-ic"><svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="17 1 21 5 17 9"/><path d="M3 11V9a4 4 0 014-4h14"/><polyline points="7 23 3 19 7 15"/><path d="M21 13v2a4 4 0 01-4 4H3"/></svg></div><div class="ni-lbl">Swaps</div></div>
    <div class="ni on"><div class="ni-ic"><svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 21v-2a4 4 0 00-4-4H8a4 4 0 00-4 4v2"/><circle cx="12" cy="7" r="4"/></svg></div><div class="ni-lbl" style="color:var(--accent)">Profile</div></div>
  </nav>
</div>

<!-- ══════════════════════════ SETTINGS ══════════════════════════ -->
<div class="scr" id="sSettings">
  <div class="topbar">
    <div class="back-bar" onclick="goBack()" style="border:none;padding:0;background:transparent">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="var(--txt2)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"/></svg>
      <span style="font-size:14px;color:var(--txt2);font-weight:500">Back</span>
    </div>
    <div class="t-brand">Settings</div>
    <div style="width:60px"></div>
  </div>
  <div class="settings-scroll">
    <div style="height:14px"></div>
    <div class="settings-sec">
      <div class="settings-sec-title">Appearance</div>
      <div class="settings-list">
        <div class="settings-item">
          <div class="s-ic"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--yellow)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/><line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/><line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/><line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/></svg></div>
          <div class="s-label">Light Mode</div>
          <label class="toggle-switch">
            <input type="checkbox" id="themeToggle" onchange="toggleTheme(this.checked)">
            <div class="toggle-track"></div>
          </label>
        </div>
      </div>
    </div>
    <div class="settings-sec">
      <div class="settings-sec-title">Account</div>
      <div class="settings-list">
        <div class="settings-item" style="cursor:default">
          <div class="s-ic"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M20 21v-2a4 4 0 00-4-4H8a4 4 0 00-4 4v2"/><circle cx="12" cy="7" r="4"/></svg></div>
          <div class="s-label">Account</div>
          <div class="s-value" id="settingsEmail">—</div>
        </div>
        <div class="settings-item" style="cursor:default">
          <div class="s-ic"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--green)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></div>
          <div class="s-label">Status</div>
          <div class="s-value" style="color:var(--green)">Active</div>
        </div>
      </div>
    </div>
    <div class="settings-sec">
      <div class="settings-sec-title">Danger Zone</div>
      <div class="settings-list">
        <div class="settings-item" onclick="doLogout()" style="cursor:pointer">
          <div class="s-ic"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M9 21H5a2 2 0 01-2-2V5a2 2 0 012-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" y1="12" x2="9" y2="12"/></svg></div>
          <div class="s-label" style="color:var(--accent)">Log Out</div>
          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>
        </div>
      </div>
    </div>
    <div style="height:24px"></div>
  </div>
</div>

<!-- ══════════════════════════ NOTIFICATIONS ══════════════════════════ -->
<div class="scr" id="sNotif">
  <div class="topbar">
    <div class="t-brand">Notifications</div>
    <div class="icon-btn" onclick="goBack()"><svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="var(--txt2)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg></div>
  </div>
  <div class="notif-scroll" id="notifList"><div class="loader"><div class="spin-ring"></div></div></div>
</div>

<!-- ══════════════════════════ VIDEO CALL ══════════════════════════ -->
<div id="callScr">
  <video id="remVid" autoplay playsinline></video>
  <div class="loc-vid-wrap"><video id="locVid" autoplay muted playsinline></video></div>
  <div class="call-overlay">
    <div class="call-top">
      <div>
        <div class="call-name" id="callName">—</div>
        <div class="call-status" id="callStatus">Connecting…</div>
      </div>
      <div class="call-timer" id="callTimer">00:00</div>
    </div>
    <div class="call-controls">
      <div class="ctrl-row">
        <button class="ctrl-btn ctrl-mute" id="btnMic" onclick="toggleMic()"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 1a3 3 0 00-3 3v8a3 3 0 006 0V4a3 3 0 00-3-3z"/><path d="M19 10v2a7 7 0 01-14 0v-2"/><line x1="12" y1="19" x2="12" y2="23"/><line x1="8" y1="23" x2="16" y2="23"/></svg></button>
        <button class="ctrl-btn ctrl-cam" id="btnCam" onclick="toggleCam()"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M15 10l4.553-2.069A1 1 0 0121 8.87v6.26a1 1 0 01-1.447.9L15 14M3 8a2 2 0 012-2h8a2 2 0 012 2v8a2 2 0 01-2 2H5a2 2 0 01-2-2V8z"/></svg></button>
        <button class="ctrl-btn ctrl-end" onclick="endCall()"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="1" y1="1" x2="23" y2="23"/><path d="M16.72 11.06A10.94 10.94 0 0119 12.55M5 12.55a10.94 10.94 0 015.17-2.39M10.71 5.05A16 16 0 0122.56 9M1.42 9a15.91 15.91 0 014.7-2.88M8.56 2.75c4.37 1.52 8.33 4.72 10.36 9.44m-1.49 5.91c-1.56 1.81-4 3.29-7.84 3.13"/></svg></button>
        <button class="ctrl-btn ctrl-spk"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="11 5 6 9 2 9 2 15 6 15 11 19 11 5"/><path d="M19.07 4.93a10 10 0 010 14.14M15.54 8.46a5 5 0 010 7.07"/></svg></button>
      </div>
    </div>
  </div>
  <div class="call-connecting" id="callConnecting">
    <div class="call-conn-spin"></div>
    <div class="call-conn-title">Connecting…</div>
    <div class="call-conn-name" id="callConnName">Waiting for the other person</div>
    <button onclick="endCall()" style="margin-top:16px;padding:9px 24px;background:rgba(231,76,60,0.2);border:1px solid rgba(231,76,60,0.4);border-radius:22px;color:#e74c3c;font-size:13px;font-weight:600;cursor:pointer">Cancel</button>
  </div>
</div>

<!-- ══════════════════════════ CHAT ══════════════════════════ -->
<div class="scr" id="sChat">
  <div class="chat-topbar">
    <div class="chat-back" onclick="goBack()">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="var(--txt2)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"/></svg>
    </div>
    <div class="chat-av" id="chatAv">?</div>
    <div class="chat-info">
      <div class="chat-name" id="chatName">—</div>
      <div class="chat-sub">Swap Active ✦</div>
    </div>
  </div>
  <div class="chat-msgs" id="chatMsgs">
    <div class="chat-empty">
      <div class="chat-empty-icon">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15a2 2 0 01-2 2H7l-4 4V5a2 2 0 012-2h14a2 2 0 012 2z"/></svg>
      </div>
      <div class="chat-empty-txt">Say hello! Start coordinating your skill swap.</div>
    </div>
  </div>
  <div class="chat-input-bar">
    <textarea class="chat-inp" id="chatInp" placeholder="Type a message…" rows="1" oninput="autoResizeChatInp(this)" onkeydown="chatEnterSend(event)"></textarea>
    <button class="chat-send" id="chatSendBtn" onclick="sendChatMsg()">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/></svg>
    </button>
  </div>
</div>

<!-- ══════════════════════════ OVERLAYS ══════════════════════════ -->
<!-- Swap Request Sheet -->
<div class="overlay" id="ovSwap" onclick="closeOv('ovSwap')">
  <div class="sheet" onclick="event.stopPropagation()">
    <div class="sheet-handle"></div>
    <div class="sheet-title">Send Swap Request</div>
    <div class="sheet-sub">Requesting a skill swap with <span id="reqName" style="color:var(--accent);font-weight:700"></span></div>
    <div class="sheet-row"><span class="sheet-key"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 19.5A2.5 2.5 0 016.5 17H20"/><path d="M6.5 2H20v20H6.5A2.5 2.5 0 014 19.5v-15A2.5 2.5 0 016.5 2z"/></svg> You Teach</span><span class="sheet-val a" id="reqTeach">—</span></div>
    <div class="sheet-row"><span class="sheet-key"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg> You Learn</span><span class="sheet-val t" id="reqLearn">—</span></div>
    <div class="sheet-row"><span class="sheet-key"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg> Safety</span><span class="sheet-val g">Verified Profile</span></div>
    <div style="margin-top:14px">
      <div class="field-lbl" style="font-size:10px;font-weight:700;color:var(--txt3);text-transform:uppercase;letter-spacing:1px;margin-bottom:6px">Message (optional)</div>
      <textarea class="textarea-inp" id="swapMessage" placeholder="Introduce yourself or share your availability…" style="margin-top:0;height:76px"></textarea>
    </div>
    <button class="btn btn-primary" style="margin-top:14px" onclick="confirmSwap()">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/></svg>
      <span id="swapBtnTxt">Send Request</span>
    </button>
    <button class="btn btn-ghost" onclick="closeOv('ovSwap')">Cancel</button>
  </div>
</div>

<!-- Report Sheet -->
<div class="overlay" id="ovReport" onclick="closeOv('ovReport')">
  <div class="sheet" onclick="event.stopPropagation()">
    <div class="sheet-handle"></div>
    <div class="sheet-title">Report User</div>
    <div class="sheet-sub">Help keep SwapSkill safe. Reports are completely confidential.</div>
    <div id="reportOptions">
      <div class="report-option" onclick="selectReport(this,'Inappropriate behavior')"><div class="report-text">Inappropriate behavior</div></div>
      <div class="report-option" onclick="selectReport(this,'Fake profile or scam')"><div class="report-text">Fake profile or scam</div></div>
      <div class="report-option" onclick="selectReport(this,'Harassment or abuse')"><div class="report-text">Harassment or abuse</div></div>
      <div class="report-option" onclick="selectReport(this,'No-show / ghosting')"><div class="report-text">No-show / ghosting</div></div>
      <div class="report-option" onclick="selectReport(this,'Other reason')"><div class="report-text">Other reason</div></div>
    </div>
    <textarea class="textarea-inp" id="reportDetail" placeholder="Optional: add more details…"></textarea>
    <button class="btn btn-danger" style="margin-top:14px;border-radius:var(--r2)" onclick="submitReport()">
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z"/></svg>
      Submit Report
    </button>
    <button class="btn btn-ghost" onclick="closeOv('ovReport')">Cancel</button>
  </div>
</div>

<!-- Rating Sheet -->
<div class="overlay" id="ovRating" onclick="closeOv('ovRating')">
  <div class="sheet" onclick="event.stopPropagation()">
    <div class="sheet-handle"></div>
    <div class="sheet-title">Rate Your Swap</div>
    <div class="sheet-sub">How was your session with <span id="rateeName" style="color:var(--accent);font-weight:700"></span>?</div>
    <div class="star-row">
      <div class="star-btn" onclick="pickStar(1)"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="var(--yellow)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg></div>
      <div class="star-btn" onclick="pickStar(2)"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="var(--yellow)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg></div>
      <div class="star-btn" onclick="pickStar(3)"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="var(--yellow)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg></div>
      <div class="star-btn" onclick="pickStar(4)"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="var(--yellow)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg></div>
      <div class="star-btn" onclick="pickStar(5)"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="var(--yellow)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg></div>
    </div>
    <textarea class="textarea-inp" id="rateComment" placeholder="Optional: leave a comment…"></textarea>
    <button class="btn btn-primary" style="margin-top:14px" onclick="submitRating()">Submit Rating</button>
  </div>
</div>

<!-- Incoming Call Overlay (Bug #12) -->
<div class="overlay" id="ovIncomingCall">
  <div class="sheet" onclick="event.stopPropagation()">
    <div class="sheet-handle"></div>
    <div class="sheet-title">📞 Incoming Call</div>
    <div class="sheet-sub"><span id="incomingCallName">Someone</span> is calling you!</div>
    <div style="display:flex;gap:10px;margin-top:16px">
      <button class="btn btn-teal" style="flex:1;border-radius:var(--r2)" onclick="answerCall()">Answer</button>
      <button class="btn btn-danger" style="flex:1;border-radius:var(--r2)" onclick="declineCall()">Decline</button>
    </div>
  </div>
</div>

</div><!-- /app -->

<!-- PWA Install Banner -->
<div id="pwaInstallBanner">
  <div class="pwa-icon">
    <svg width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="17 1 21 5 17 9"/><path d="M3 11V9a4 4 0 014-4h14"/><polyline points="7 23 3 19 7 15"/><path d="M21 13v2a4 4 0 01-4 4H3"/></svg>
  </div>
  <div class="pwa-text">
    <div class="pwa-title">Install SwapSkill</div>
    <div class="pwa-sub">Add to home screen for the full app experience — works offline too!</div>
  </div>
  <div class="pwa-actions">
    <button class="pwa-btn-install" id="pwaInstallBtn">Install</button>
    <button class="pwa-btn-dismiss" id="pwaDismissBtn">Not now</button>
  </div>
</div>

<script>
'use strict';

// dismissInstantSplash — called once by go() when first real screen appears
function dismissInstantSplash() {
  const is = document.getElementById('instantSplash');
  if (is && !is._dismissed) {
    is._dismissed = true;
    is.classList.add('fade-out');
    setTimeout(() => { try { is.parentNode.removeChild(is); } catch(e) {} }, 420);
  }
}
/* ══════════════════════════════════════════════════════════════
   FIREBASE — Correct configuration
══════════════════════════════════════════════════════════════ */
// REQUIRED Firestore Security Rules (paste into Firebase Console → Firestore → Rules):
// rules_version = '2';
// service cloud.firestore {
//   match /databases/{database}/documents {
//     match /users/{uid} {
//       allow read: if request.auth != null;
//       allow write: if request.auth != null && request.auth.uid == uid;
//     }
//     match /swaps/{swapId} {
//       allow read: if request.auth != null;
//       allow create: if request.auth != null;
//       allow update: if request.auth != null &&
//         (resource.data.fromUid == request.auth.uid || resource.data.toUid == request.auth.uid);
//       match /messages/{msgId} {
//         allow read, create: if request.auth != null;
//       }
//     }
//     match /notifications/{notifId} {
//       allow read: if request.auth != null && resource.data.toUid == request.auth.uid;
//       allow create: if request.auth != null;
//       allow update: if request.auth != null && resource.data.toUid == request.auth.uid;
//     }
//     match /reports/{reportId} {
//       allow create: if request.auth != null;
//     }
//     match /calls/{callId} {
//       allow read, write: if request.auth != null;
//       match /ice_caller/{iceId} { allow read, write: if request.auth != null; }
//       match /ice_callee/{iceId} { allow read, write: if request.auth != null; }
//     }
//   }
// }
const FB_CONFIG = {
  apiKey: "AIzaSyCLjbsf4jyP8Mwpn7ev60PongoON1oRELk",
  authDomain: "swapskills-7431b.firebaseapp.com",
  projectId: "swapskills-7431b",
  storageBucket: "swapskills-7431b.appspot.com",
  messagingSenderId: "565562919586",
  appId: "1:565562919586:web:809341395a856a3651e5fb"
};

let AUTH, DB;
try {
  if (typeof firebase === 'undefined') throw new Error('Firebase SDK not loaded');
  firebase.initializeApp(FB_CONFIG);
  AUTH = firebase.auth();
  DB   = firebase.firestore();
  // Enable offline persistence
  DB.enablePersistence({ synchronizeTabs: true }).catch(e => {
    if (e.code === 'failed-precondition') {
      console.warn('Firestore: Multiple tabs open, persistence disabled.');
    } else if (e.code === 'unimplemented') {
      console.warn('Firestore: Browser does not support persistence.');
    }
  });
} catch(fbErr) {
  console.error('Firebase failed to initialize:', fbErr.message);
  // SDK failed (e.g. blocked CDN in sandboxed preview) — go to auth immediately, no blank wait
  setTimeout(() => {
    go('sAuth');
    const errEl = document.getElementById('authErr');
    if (errEl) {
      errEl.textContent = 'Could not connect to server. Check internet and reload.';
      errEl.classList.add('on');
    }
  }, 0);
}

/* ══════════════════════════════════════════════════════════════
   SKILLS MASTER LIST
══════════════════════════════════════════════════════════════ */
const SKILLS = [
  {l:'Python',c:'Tech'},{l:'JavaScript',c:'Tech'},{l:'Web Development',c:'Tech'},
  {l:'React / Next.js',c:'Tech'},{l:'Data Science',c:'Tech'},{l:'Machine Learning',c:'Tech'},
  {l:'SQL & Databases',c:'Tech'},{l:'Cybersecurity',c:'Tech'},{l:'Cloud (AWS/GCP)',c:'Tech'},
  {l:'Mobile Apps',c:'Tech'},{l:'UI/UX Design',c:'Tech'},{l:'WordPress',c:'Tech'},
  {l:'Video Editing',c:'Tech'},{l:'3D Modeling',c:'Tech'},{l:'Graphic Design',c:'Tech'},
  {l:'Excel & Sheets',c:'Tech'},{l:'Figma',c:'Tech'},{l:'DevOps',c:'Tech'},
  {l:'Guitar',c:'Music'},{l:'Piano',c:'Music'},{l:'Violin',c:'Music'},{l:'Tabla',c:'Music'},
  {l:'Singing / Vocals',c:'Music'},{l:'Music Production',c:'Music'},{l:'Drums',c:'Music'},
  {l:'Flute',c:'Music'},{l:'Sitar',c:'Music'},{l:'DJing',c:'Music'},{l:'Music Theory',c:'Music'},
  {l:'Pakistani Cooking',c:'Cooking'},{l:'Arabic Cooking',c:'Cooking'},{l:'Baking & Pastry',c:'Cooking'},
  {l:'BBQ & Grilling',c:'Cooking'},{l:'Sushi & Japanese',c:'Cooking'},{l:'Vegan Cooking',c:'Cooking'},
  {l:'Italian Cooking',c:'Cooking'},{l:'Chinese Cooking',c:'Cooking'},{l:'Meal Prep',c:'Cooking'},
  {l:'English Speaking',c:'Language'},{l:'Urdu',c:'Language'},{l:'Arabic',c:'Language'},
  {l:'French',c:'Language'},{l:'Spanish',c:'Language'},{l:'Chinese (Mandarin)',c:'Language'},
  {l:'German',c:'Language'},{l:'Turkish',c:'Language'},{l:'Japanese',c:'Language'},
  {l:'Hindi',c:'Language'},{l:'Persian / Farsi',c:'Language'},{l:'Korean',c:'Language'},
  {l:'Drawing & Sketching',c:'Art'},{l:'Calligraphy',c:'Art'},{l:'Watercolor',c:'Art'},
  {l:'Photography',c:'Art'},{l:'Filmmaking',c:'Art'},{l:'Pottery',c:'Art'},
  {l:'Embroidery',c:'Art'},{l:'Digital Art',c:'Art'},{l:'Acting & Drama',c:'Art'},
  {l:'Stock Trading',c:'Finance'},{l:'Crypto Basics',c:'Finance'},{l:'Budgeting',c:'Finance'},
  {l:'Accounting',c:'Finance'},{l:'Real Estate',c:'Finance'},{l:'Freelancing',c:'Finance'},
  {l:'Yoga',c:'Fitness'},{l:'Weight Training',c:'Fitness'},{l:'Calisthenics',c:'Fitness'},
  {l:'Running',c:'Fitness'},{l:'Martial Arts',c:'Fitness'},{l:'Swimming',c:'Fitness'},
  {l:'Nutrition',c:'Fitness'},{l:'Meditation',c:'Fitness'},
  {l:'Mathematics',c:'Academic'},{l:'Physics',c:'Academic'},{l:'Chemistry',c:'Academic'},
  {l:'Biology',c:'Academic'},{l:'History',c:'Academic'},{l:'Essay Writing',c:'Academic'},
  {l:'Public Speaking',c:'Academic'},{l:'Research Skills',c:'Academic'},
];

const AVATAR_COLORS = [
  '#e74c3c','#e67e22','#f39c12','#27ae60','#16a085',
  '#2980b9','#8e44ad','#c0392b','#d35400','#2c3e50'
];

/* ══════════════════════════════════════════════════════════════
   APP STATE
══════════════════════════════════════════════════════════════ */
let CU   = null; // current Firebase user
let CUD  = null; // current user Firestore data
let prevScreen = 'sHome';
let curScreen  = 'sSplash';
let curFilter  = 'All';
let allUsers   = [];
let userCache  = {};
let curTarget  = null; // user being viewed in detail/swap
let curSwapId  = null;
let reportReason = '';
let starRating = 0;
let isNewUser  = false;
let curSwapTab = 'received';
let isDark     = true;

// WebRTC
let localStream = null, peerConn = null;
let micMuted = false, camOff = false;
let callTimerInt = null, callSecs = 0;

/* ══════════════════════════════════════════════════════════════
   NAVIGATION
══════════════════════════════════════════════════════════════ */
function go(screenId) {
  if (!screenId || screenId === curScreen) return;
  // Prevent navigating back to splash after app is loaded
  if (screenId === 'sSplash' && curScreen !== 'sSplash') return;
  // Unsubscribe chat listener when leaving chat
  if (curScreen === 'sChat' && screenId !== 'sChat' && chatUnsub) {
    chatUnsub(); chatUnsub = null;
  }
  prevScreen = curScreen;
  curScreen  = screenId;
  document.querySelectorAll('.scr').forEach(s => s.classList.remove('on'));
  const el = document.getElementById(screenId);
  if (el) el.classList.add('on');
  // Dismiss the instant splash overlay the moment any real screen is shown
  // Keep it alive during 'sSplash' so there's no gap between CSS splash and JS splash
  if (screenId !== 'sSplash') dismissInstantSplash();
}

function navTo(screenId, initFn) {
  go(screenId);
  if (initFn) try { initFn(); } catch(e) { console.warn('navTo init:', e); }
}

function goBack() {
  const safe = ['sHome','sExplore','sSwaps','sProfile','sNotif','sLboard'];
  const dest = safe.includes(prevScreen) ? prevScreen : 'sHome';
  go(dest);
}

/* ══════════════════════════════════════════════════════════════
   THEME
══════════════════════════════════════════════════════════════ */
function toggleTheme(isLight) {
  isDark = !isLight;
  document.documentElement.setAttribute('data-theme', isDark ? 'dark' : 'light');
  try { localStorage.setItem('ss_theme', isDark ? 'dark' : 'light'); } catch(e) {}
}

// Restore saved theme
(function () {
  try {
    const saved = localStorage.getItem('ss_theme');
    if (saved === 'light') {
      isDark = false;
      document.documentElement.setAttribute('data-theme', 'light');
    }
  } catch(e) {}
})();

/* ══════════════════════════════════════════════════════════════
   FIREBASE AUTH STATE
══════════════════════════════════════════════════════════════ */
go('sSplash');

// Splash screen timeout — if Firebase never responds, redirect to auth
const splashTimeout = setTimeout(() => {
  if (curScreen === 'sSplash') {
    go('sAuth');
    // Only show error toast if Firebase was supposed to be available
    if (AUTH) toast('Connection is slow. Please check your internet.', 'er');
  }
}, 6000); // 6s gives Firebase enough time on slow connections

if (AUTH) {
AUTH.onAuthStateChanged(async user => {
  clearTimeout(splashTimeout);
  if (user) {
    CU = user;
    try {
      const snap = await DB.collection('users').doc(user.uid).get();
      CUD = snap.exists ? snap.data() : null;
      // Live updates
      DB.collection('users').doc(user.uid)
        .onSnapshot(d => { if (d.exists) { CUD = d.data(); if (curScreen === 'sProfile') refreshProfile(); }});
    } catch(e) { console.warn('User fetch:', e.code); }

    if (isNewUser || !CUD || !(CUD.teaches || []).length) {
      isNewUser = false;
      buildOnbPills();
      go('sOnboard');
    } else {
      go('sHome');
      initApp();
    }
  } else {
    CU = null; CUD = null;
    go('sAuth');
  }
});
} // end if(AUTH)

/* ══════════════════════════════════════════════════════════════
   APP INIT
══════════════════════════════════════════════════════════════ */
function initApp() {
  refreshProfile();
  loadHome();
  loadExplore();
  buildSetupPills();
  startNotifListener();
  loadStats();
  startIncomingCallListener();
}

/* ══════════════════════════════════════════════════════════════
   AUTH FUNCTIONS
══════════════════════════════════════════════════════════════ */
function authTab(t) {
  document.getElementById('tabLogin').classList.toggle('on', t==='login');
  document.getElementById('tabSignup').classList.toggle('on', t==='signup');
  document.getElementById('fLogin').classList.toggle('on', t==='login');
  document.getElementById('fSignup').classList.toggle('on', t==='signup');
  hideAuthErr();
}

function showAuthErr(msg) {
  const el = document.getElementById('authErr');
  el.textContent = msg; el.classList.add('on');
}
function hideAuthErr() { document.getElementById('authErr').classList.remove('on'); }

function authErrMsg(code) {
  const M = {
    'auth/user-not-found': 'No account found with this email. Please sign up first.',
    'auth/wrong-password': 'Incorrect password. Please try again.',
    'auth/invalid-credential': 'Incorrect email or password. Please check and try again.',
    'auth/invalid-email': 'Please enter a valid email address.',
    'auth/email-already-in-use': 'This email is already registered. Sign in instead.',
    'auth/weak-password': 'Password must be at least 6 characters.',
    'auth/too-many-requests': 'Too many failed attempts. Please wait a few minutes and try again.',
    'auth/network-request-failed': 'No internet connection. Please check your network and try again.',
    'auth/user-disabled': 'This account has been disabled. Please contact support.',
    'auth/operation-not-allowed': 'Email/password sign-in is not enabled. Please contact support.',
  };
  return M[code] || 'Something went wrong. Please try again.';
}

async function doLogin() {
  const email = $v('lEmail'), pass = $v('lPass');
  if (!email || !pass) { showAuthErr('Please fill in all fields.'); return; }
  if (!AUTH) { showAuthErr('Not connected to server. Please reload the page.'); return; }
  setBtn('btnLogin','btnLoginTxt','Signing in…',true);
  try {
    await AUTH.signInWithEmailAndPassword(email, pass);
    toast('Welcome back!','ok');
  } catch(e) { showAuthErr(authErrMsg(e.code)); }
  finally { setBtn('btnLogin','btnLoginTxt','Sign In',false); }
}

async function doSignup() {
  const name = $v('sName'), email = $v('sEmail'), pass = $v('sPass');
  if (!name || !email || !pass) { showAuthErr('Please fill in all fields.'); return; }
  if (pass.length < 6) { showAuthErr('Password must be at least 6 characters.'); return; }
  if (!AUTH) { showAuthErr('Not connected to server. Please reload the page.'); return; }
  setBtn('btnSignup','btnSignupTxt','Creating account…',true);
  try {
    const cred = await AUTH.createUserWithEmailAndPassword(email, pass);
    await cred.user.updateProfile({ displayName: name });
    const initials = name.split(' ').map(w=>w[0]).join('').toUpperCase().slice(0,2);
    const color = AVATAR_COLORS[Math.floor(Math.random()*AVATAR_COLORS.length)];
    await DB.collection('users').doc(cred.user.uid).set({
      uid: cred.user.uid, name, email,
      teaches: [], learns: [], bio: '',
      swapsCompleted: 0, rating: 0, ratingCount: 0, streak: 0,
      verified: false, initials, avatarColor: color,
      createdAt: firebase?.firestore?.FieldValue.serverTimestamp(),
    });
    isNewUser = true;
    toast('Account created!','ok');
  } catch(e) { showAuthErr(authErrMsg(e.code)); }
  finally { setBtn('btnSignup','btnSignupTxt','Create Account',false); }
}


async function doLogout() {
  await AUTH.signOut();
  toast('Signed out. See you soon!');
}

/* ══════════════════════════════════════════════════════════════
   ONBOARDING
══════════════════════════════════════════════════════════════ */
function buildOnbPills() {
  buildPills('onbTeachPills','p-teach');
  buildPills('onbLearnPills','p-learn');
}

function onbNext() {
  if (!document.querySelectorAll('#onbTeachPills .p-teach').length) {
    toast('Pick at least 1 skill to teach'); return;
  }
  $('onbP1').style.display = 'none';
  $('onbP2').style.display = 'block';
  $('onbStep').textContent = 'Step 2 of 2';
  $('os2').classList.add('done');
}
function onbBack() {
  $('onbP2').style.display = 'none';
  $('onbP1').style.display = 'block';
  $('onbStep').textContent = 'Step 1 of 2';
  $('os2').classList.remove('done');
}
async function onbFinish() {
  const teaches = getPillValues('onbTeachPills','p-teach');
  const learns  = getPillValues('onbLearnPills','p-learn');
  if (!learns.length) { toast('Pick at least 1 skill to learn'); return; }
  $('onbFinTxt').textContent = 'Saving…';
  try {
    await DB.collection('users').doc(CU.uid).update({ teaches, learns });
    CUD = { ...CUD, teaches, learns };
    go('sHome'); initApp();
    toast('Profile ready! Finding matches…','ok');
  } catch(e) {
    $('onbFinTxt').textContent = 'Finish & Find Matches';
    toast('Error saving profile. Please check your connection and try again.','er');
  }
}

/* ══════════════════════════════════════════════════════════════
   PILLS (skill selectors)
══════════════════════════════════════════════════════════════ */
function buildPills(containerId, cls) {
  const el = $(containerId); if (!el) return;
  el.innerHTML = SKILLS.map(s =>
    `<div class="pill" data-l="${s.l}" data-c="${s.c}" onclick="togglePill(this,'${containerId}','${cls}')">${s.l}</div>`
  ).join('');
}

function togglePill(el, containerId, cls) {
  const current = document.querySelectorAll(`#${containerId} .${cls}`).length;
  if (el.classList.contains(cls)) {
    el.classList.remove(cls);
  } else if (current < 3) {
    el.classList.add(cls);
  } else {
    toast('Maximum 3 skills allowed'); return;
  }
  updatePillCount(containerId, cls);
}

function updatePillCount(containerId, cls) {
  const n = document.querySelectorAll(`#${containerId} .${cls}`).length;
  const map = {
    'onbTeachPills':'onbTCnt','onbLearnPills':'onbLCnt',
    'setupTeachPills':'stpTCnt','setupLearnPills':'stpLCnt'
  };
  const el = $(map[containerId]);
  if (el) { el.textContent = `${n} / 3 selected`; el.className = `sel-count${n>0?' active':''}`; }
}

function filterPills(containerId, query, cls) {
  const q = query.toLowerCase();
  document.querySelectorAll(`#${containerId} .pill`).forEach(p => {
    p.classList.toggle('hidden', q.length > 0 && !p.dataset.l.toLowerCase().includes(q));
  });
}

function getPillValues(containerId, cls) {
  return [...document.querySelectorAll(`#${containerId} .${cls}`)].map(e => e.dataset.l);
}

function buildSetupPills() {
  buildPills('setupTeachPills','p-teach');
  buildPills('setupLearnPills','p-learn');
  if (!CUD) return;
  (CUD.teaches||[]).forEach(s => {
    const el = document.querySelector(`#setupTeachPills [data-l="${CSS.escape(s)}"]`);
    if (el) el.classList.add('p-teach');
  });
  (CUD.learns||[]).forEach(s => {
    const el = document.querySelector(`#setupLearnPills [data-l="${CSS.escape(s)}"]`);
    if (el) el.classList.add('p-learn');
  });
  updatePillCount('setupTeachPills','p-teach');
  updatePillCount('setupLearnPills','p-learn');
  if (CUD.bio) $('setupBio').value = CUD.bio;
}

/* ══════════════════════════════════════════════════════════════
   HOME
══════════════════════════════════════════════════════════════ */
function loadHome() {
  if (!CUD) return;
  const first = (CUD.name||'').split(' ')[0] || 'there';
  $('heroName').innerHTML = `Hey ${x(first)}, find your <span>Skill Swap</span>`;
  const chips = [...(CUD.teaches||[]), ...(CUD.learns||[])].slice(0,6);
  $('heroChips').innerHTML = (chips.length ? chips : ['Guitar','Python','Cooking','Design','Languages'])
    .map(c => `<div class="chip">${x(c)}</div>`).join('');
  loadMatches();
}

async function loadMatches() {
  const el = $('homeMatches');
  el.innerHTML = '<div class="loader"><div class="spin-ring"></div> Loading…</div>';
  try {
    const snap = await DB.collection('users').limit(60).get();
    const users = snap.docs.map(d=>d.data()).filter(u=>u.uid!==CU?.uid&&(u.teaches||[]).length);
    const learns = CUD?.learns||[];
    const scored = users.map(u => ({ ...u, score:(u.teaches||[]).filter(t=>learns.includes(t)).length }))
      .sort((a,b)=>b.score-a.score||(b.swapsCompleted||0)-(a.swapsCompleted||0)).slice(0,10);
    if (!scored.length) {
      el.innerHTML = '<div style="padding:16px 8px;font-size:13px;color:var(--txt3)">No matches yet — explore all users</div>';
      return;
    }
    el.innerHTML = scored.map(u => {
      const pct = Math.min(98, 55 + u.score*20 + Math.min(u.swapsCompleted||0,10));
      userCache[u.uid] = u;
      return `<div class="match-card" onclick="openDetail('${u.uid}')">
        <div class="m-avatar" style="background:${u.avatarColor||AVATAR_COLORS[0]}">${x(u.initials||'?')}</div>
        <div class="m-name">${x(u.name)}</div>
        <div class="m-teach">${x((u.teaches||['—'])[0])}</div>
        <div class="m-learn">${x((u.learns||['—'])[0])}</div>
        <div class="m-bar-row"><span class="m-pct">${pct}%</span><div class="m-bar"><div class="m-bar-fill" style="width:${pct}%"></div></div></div>
      </div>`;
    }).join('');
  } catch(e) {
    el.innerHTML = '<div class="error-state">Could not load matches.<br><small>'+x(e.message)+'</small></div>';
  }
}

async function loadStats() {
  try {
    const snap = await DB.collection('users').get();
    const users = snap.docs.map(d=>d.data());
    $('statUsers').textContent = fmtNum(users.length);
    $('statSwaps').textContent = fmtNum(users.reduce((s,u)=>s+(u.swapsCompleted||0),0));
  } catch(e) {}
}

/* ══════════════════════════════════════════════════════════════
   EXPLORE
══════════════════════════════════════════════════════════════ */
async function loadExplore() {
  if (!CU) return;
  $('expList').innerHTML = '<div class="loader"><div class="spin-ring"></div> Loading people…</div>';
  try {
    const snap = await DB.collection('users').limit(100).get();
    // BUG FIX: was missing assignment — allUsers was always empty
    allUsers = snap.docs.map(d => d.data()).filter(u => u.uid && u.uid !== CU.uid);
    allUsers.forEach(u => { if(u.uid) userCache[u.uid]=u; });
    renderCards();
  } catch(e) {
    $('expList').innerHTML = `<div class="error-state">Failed to load.<br><small>${x(e.message)}</small><br><br><button onclick="loadExplore()" style="padding:9px 18px;background:var(--accent);color:#fff;border:none;border-radius:10px;font-size:13px;font-weight:600;cursor:pointer">Retry</button></div>`;
  }
}

function renderCards() {
  const el = $('expList');
  const q = ($('searchInp')?.value||'').toLowerCase().trim();
  let list = allUsers.filter(u=>(u.teaches||[]).length);

  if (curFilter !== 'All') {
    list = list.filter(u=>[...(u.teaches||[]),...(u.learns||[])].some(s=>{
      const sk = SKILLS.find(sk=>sk.l===s); return sk && sk.c===curFilter;
    }));
  }
  if (q) {
    list = list.filter(u=>
      (u.name||'').toLowerCase().includes(q)||
      (u.teaches||[]).some(s=>s.toLowerCase().includes(q))||
      (u.learns||[]).some(s=>s.toLowerCase().includes(q))
    );
  }
  if (!list.length) {
    el.innerHTML = `<div class="empty-state">
      <div class="empty-icon"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg></div>
      <div class="empty-title">No users found</div>
      <div class="empty-sub">${curFilter!=='All'?'Try a different category':'Be the first to join!'}</div>
    </div>`; return;
  }
  el.innerHTML = list.map(u => {
    userCache[u.uid] = u;
    const te = x((u.teaches||['—'])[0]);
    const wa = x((u.learns||['—'])[0]);
    const rat = u.ratingCount>0 ? (u.rating/u.ratingCount).toFixed(1) : 'New';
    return `<div class="sk-card" onclick="openDetail('${u.uid}')">
      <div class="sk-top">
        <div class="sk-av" style="background:${u.avatarColor||AVATAR_COLORS[0]}">${x(u.initials||'?')}</div>
        <div style="flex:1;min-width:0">
          <div class="sk-name">${x(u.name)}</div>
          <div class="sk-meta">
            ${u.verified?'<span class="badge badge-green">Verified</span>':''}
            ${(u.swapsCompleted||0)>4?'<span class="badge badge-teal">Trusted</span>':''}
            <span>${u.swapsCompleted||0} swaps</span>
          </div>
        </div>
      </div>
      <div class="swap-panel">
        <div class="swap-side"><div class="swap-side-lbl teach">Teaches</div><div class="swap-side-val">${te}</div></div>
        <div class="swap-ic"><svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="17 1 21 5 17 9"/><path d="M3 11V9a4 4 0 014-4h14"/><polyline points="7 23 3 19 7 15"/><path d="M21 13v2a4 4 0 01-4 4H3"/></svg></div>
        <div class="swap-side"><div class="swap-side-lbl learn">Wants</div><div class="swap-side-val">${wa}</div></div>
      </div>
      <div class="sk-footer">
        <div class="rating-row"><svg width="12" height="12" viewBox="0 0 24 24" fill="var(--yellow)" stroke="var(--yellow)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg> ${rat}</div>
        <button class="swap-btn" onclick="event.stopPropagation();openSwapForUid('${u.uid}')">
          <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="17 1 21 5 17 9"/><path d="M3 11V9a4 4 0 014-4h14"/><polyline points="7 23 3 19 7 15"/><path d="M21 13v2a4 4 0 01-4 4H3"/></svg>
          Swap
        </button>
      </div>
    </div>`;
  }).join('');
}

function setFilter(el, cat) {
  document.querySelectorAll('.fchip').forEach(c=>c.classList.remove('on'));
  el.classList.add('on'); curFilter = cat; renderCards();
}

/* ══════════════════════════════════════════════════════════════
   DETAIL
══════════════════════════════════════════════════════════════ */
let detailUser = null;

async function openDetail(uid) {
  prevScreen = curScreen; go('sDetail');
  // Use cache instantly, then update from Firestore
  if (userCache[uid]) renderDetail(userCache[uid]);
  try {
    const snap = await DB.collection('users').doc(uid).get();
    if (snap.exists) {
      detailUser = snap.data();
      userCache[uid] = detailUser;
      renderDetail(detailUser);
    } else if (!userCache[uid]) { toast('User not found','er'); goBack(); }
  } catch(e) {
    if (!userCache[uid]) { toast('Error loading profile','er'); goBack(); }
  }
}

function renderDetail(u) {
  detailUser = u;
  $('detAv').textContent = u.initials||'?';
  $('detAv').style.background = u.avatarColor||AVATAR_COLORS[0];
  $('detName').textContent = u.name||'Unknown';
  const rat = u.ratingCount>0 ? (u.rating/u.ratingCount).toFixed(1) : 'New';
  $('detRating').textContent = rat==='New'?'New':'★ '+rat;
  $('detSwaps').textContent = u.swapsCompleted||0;
  $('detYear').textContent = u.createdAt ? new Date(u.createdAt.seconds*1000).getFullYear() : '—';
  $('detBio').textContent = u.bio||'No bio yet.';
  $('detTeach').textContent = (u.teaches||['—']).join(', ');
  $('detLearn').textContent = (u.learns||['—']).join(', ');
  $('detBadges').innerHTML = [
    u.verified ? '<span class="badge badge-green">Verified</span>' : '',
    (u.swapsCompleted||0)>4 ? '<span class="badge badge-teal">Trusted</span>' : '',
    u.createdAt ? `<span style="font-size:12px;color:var(--txt3)">Since ${new Date(u.createdAt.seconds*1000).getFullYear()}</span>` : ''
  ].filter(Boolean).join('');
}

async function openSwapForUid(uid) {
  let u = userCache[uid];
  if (!u) {
    try {
      const snap = await DB.collection('users').doc(uid).get();
      if (snap.exists) { u = snap.data(); userCache[uid] = u; }
    } catch(e) { toast('Could not load user. Try again.', 'er'); return; }
  }
  if (u) openSwapWith(u);
  else toast('User not found.', 'er');
}

/* ══════════════════════════════════════════════════════════════
   SWAP REQUEST
══════════════════════════════════════════════════════════════ */
function openSwapRequest() { openSwapWith(detailUser); }

function openSwapWith(u) {
  if (!CU) { toast('Please sign in first'); return; }
  if (!(CUD?.teaches||[]).length) { toast('Set up your skills first — go to Post tab'); return; }
  if (!u || !u.uid) { toast('User data incomplete. Please tap the profile first.', 'er'); return; }
  curTarget = u;
  const msgEl = $('swapMessage'); if (msgEl) msgEl.value = '';
  const nameEl = $('reqName'); if(nameEl) nameEl.textContent = u.name;
  const teachEl = $('reqTeach'); if(teachEl) teachEl.textContent = (CUD.teaches||['—'])[0];
  const learnEl = $('reqLearn'); if(learnEl) learnEl.textContent = (u.teaches||['—'])[0];
  $('ovSwap').classList.add('on');
}

function pickSession(el) {
  document.querySelectorAll('.sess-option').forEach(s=>s.classList.remove('on'));
  el.classList.add('on');
}

async function confirmSwap() {
  if (!CU || !curTarget) return;
  // Production req: prevent sending a swap request to yourself
  if (curTarget.uid === CU.uid) { toast("You can't swap with yourself!"); return; }

  const btn = document.querySelector('#ovSwap .btn-primary');

  // Bug #1: Re-verify CUD is loaded before proceeding
  if (!CUD || !CUD.name) {
    try {
      const snap = await DB.collection('users').doc(CU.uid).get();
      if (snap.exists) CUD = snap.data();
      else { toast('Your profile is not set up. Please complete setup first.', 'er'); return; }
    } catch(e) { toast('Could not load your profile. Try again.', 'er'); return; }
  }

  const swapMsg = ($('swapMessage')?.value||'').trim();
  const swapBtnTxt = $('swapBtnTxt');
  if(swapBtnTxt) swapBtnTxt.textContent = 'Sending…';
  if (btn) btn.disabled = true;

  try {
    // Bug #7: Duplicate swap check
    const existing = await DB.collection('swaps')
      .where('fromUid','==',CU.uid)
      .where('toUid','==',curTarget.uid)
      .where('status','==','pending')
      .get();
    if (!existing.empty) {
      toast('You already have a pending request with this person.', 'er');
      closeOv('ovSwap');
      return;
    }

    const swapRef = await DB.collection('swaps').add({
      fromUid: CU.uid,
      fromName: CUD.name||'',
      fromInitials: CUD.initials||'?',
      fromColor: CUD.avatarColor||AVATAR_COLORS[0],
      fromTeaches: CUD.teaches||[],
      toUid: curTarget.uid,
      toName: curTarget.name||'',
      toInitials: curTarget.initials||'?',
      toColor: curTarget.avatarColor||AVATAR_COLORS[0],
      toTeaches: curTarget.teaches||[],
      message: swapMsg,
      status: 'pending',
      createdAt: firebase?.firestore?.FieldValue.serverTimestamp(),
    });

    // Bug #15: Verify write reached server
    const verify = await DB.collection('swaps').doc(swapRef.id).get({ source: 'server' });
    if (!verify.exists) throw new Error('Write not confirmed by server.');

    // Notification
    await DB.collection('notifications').add({
      toUid: curTarget.uid,
      fromUid: CU.uid,
      fromName: CUD.name,
      type: 'swap_request',
      swapId: swapRef.id,
      swapMessage: swapMsg || '',
      message: swapMsg
        ? `${x(CUD.name)} wants to swap skills with you!`
        : `${x(CUD.name)} wants to swap skills with you!`,
      read: false,
      createdAt: firebase?.firestore?.FieldValue.serverTimestamp(),
    });
    closeOv('ovSwap');
    toast('Swap request sent!','ok');
  } catch(e) {
    // Bug #1: Better error messages
    const msg = e.code === 'permission-denied'
      ? 'Permission denied. Make sure your Firestore rules allow writes.'
      : e.code === 'unavailable'
      ? 'No internet connection. Please check your network.'
      : `Failed to send: ${e.message}`;
    toast(msg, 'er');
    console.error('confirmSwap full error:', e.code, e.message, e);
  } finally {
    // Bug #6: Always re-enable button
    if(swapBtnTxt) swapBtnTxt.textContent = 'Send Request';
    if (btn) btn.disabled = false;
  }
}

/* ══════════════════════════════════════════════════════════════
   MY SWAPS — All queries use simple single-field where clauses
   merged client-side. No composite indexes needed.
══════════════════════════════════════════════════════════════ */
function swapsTab(el, tab) {
  document.querySelectorAll('.sw-tab').forEach(t=>t.classList.remove('on'));
  el.classList.add('on'); curSwapTab = tab; loadSwaps();
}

async function loadSwaps() {
  if (!CU) return;
  const el = $('swapsList');
  el.innerHTML = '<div class="loader"><div class="spin-ring"></div> Loading…</div>';
  try {
    let items = [];
    const uid = CU.uid;

    if (curSwapTab === 'received') {
      // Pending swaps sent TO me
      const snap = await DB.collection('swaps').where('toUid','==',uid).where('status','==','pending').get();
      items = snap.docs.map(d=>({id:d.id,...d.data()}));
    } else if (curSwapTab === 'sent') {
      // Pending swaps I sent
      const snap = await DB.collection('swaps').where('fromUid','==',uid).where('status','==','pending').get();
      items = snap.docs.map(d=>({id:d.id,...d.data()}));
    } else if (curSwapTab === 'active') {
      // Accepted swaps I'm involved in — run both queries, merge
      const [s1, s2] = await Promise.all([
        DB.collection('swaps').where('toUid','==',uid).where('status','==','accepted').get(),
        DB.collection('swaps').where('fromUid','==',uid).where('status','==','accepted').get(),
      ]);
      const seen = new Set();
      [...s1.docs,...s2.docs].forEach(d=>{ if(!seen.has(d.id)){seen.add(d.id);items.push({id:d.id,...d.data()});}});
    } else {
      // Completed swaps I'm involved in
      const [s1, s2] = await Promise.all([
        DB.collection('swaps').where('toUid','==',uid).where('status','==','completed').get(),
        DB.collection('swaps').where('fromUid','==',uid).where('status','==','completed').get(),
      ]);
      const seen = new Set();
      [...s1.docs,...s2.docs].forEach(d=>{ if(!seen.has(d.id)){seen.add(d.id);items.push({id:d.id,...d.data()});}});
    }

    // Client-side sort by createdAt descending
    items.sort((a,b)=>(b.createdAt?.seconds||0)-(a.createdAt?.seconds||0));

    if (!items.length) {
      el.innerHTML = `<div class="empty-state">
        <div class="empty-icon"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="var(--txt3)" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="17 1 21 5 17 9"/><path d="M3 11V9a4 4 0 014-4h14"/><polyline points="7 23 3 19 7 15"/><path d="M21 13v2a4 4 0 01-4 4H3"/></svg></div>
        <div class="empty-title">No ${curSwapTab} swaps</div>
        <div class="empty-sub">Explore people and send your first swap request!</div>
      </div>`; return;
    }
    el.innerHTML = items.map(sw => renderSwapItem(sw)).join('');

    // For accepted swaps, check message count and unlock Rate button if >= 3 exchanges
    if (curSwapTab === 'active') {
      items.forEach(sw => checkAndUnlockRating(sw.id));
    }

    // Update pending badge
    if (curSwapTab === 'received') {
      const badge = $('pendingBadge');
      if (badge) { badge.textContent = items.length; badge.style.display = items.length ? 'block':'none'; }
    }
  } catch(e) {
    el.innerHTML = `<div class="error-state">Error loading swaps.<br><small>${x(e.message)}</small><br><br><button onclick="loadSwaps()" style="padding:9px 18px;background:var(--accent);color:#fff;border:none;border-radius:10px;font-size:13px;font-weight:600;cursor:pointer">Retry</button></div>`;
    console.error('loadSwaps:', e);
  }
}

function renderSwapItem(sw) {
  const isFrom = sw.fromUid === CU?.uid;
  const otherName = isFrom ? sw.toName : sw.fromName;
  const otherInit = isFrom ? sw.toInitials : sw.fromInitials;
  const otherColor = isFrom ? sw.toColor : sw.fromColor;
  const myTeach   = isFrom ? (sw.fromTeaches||['—'])[0] : (sw.toTeaches||['—'])[0];
  const theirTeach= isFrom ? (sw.toTeaches||['—'])[0] : (sw.fromTeaches||['—'])[0];
  const otherUid  = isFrom ? sw.toUid : sw.fromUid;
  const stMap = {pending:'st-pending',accepted:'st-accepted',completed:'st-completed',declined:'st-declined'};
  let actions = '';
  if (sw.status==='pending' && !isFrom) {
    actions = `<div class="sw-actions">
      <button class="sw-act-btn sa-accept" onclick="respondSwap('${sw.id}','accepted')">
        <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg> Accept
      </button>
      <button class="sw-act-btn sa-decline" onclick="respondSwap('${sw.id}','declined')">
        <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg> Decline
      </button>
    </div>`;
  } else if (sw.status==='accepted') {
    const chatDataAttrs = `data-swid="${sw.id}" data-uid="${otherUid}" data-name="${(otherName||'').replace(/"/g,'&quot;')}" data-init="${(otherInit||'?').replace(/"/g,'&quot;')}" data-color="${(otherColor||AVATAR_COLORS[0]).replace(/"/g,'&quot;')}"`;
    const rateDataAttrs = `data-swid="${sw.id}" data-name="${(otherName||'').replace(/"/g,'&quot;')}"`;
    actions = `<div class="sw-actions">
      <button class="sw-act-btn sa-call sw-chat-btn" ${chatDataAttrs}>
        <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15a2 2 0 01-2 2H7l-4 4V5a2 2 0 012-2h14a2 2 0 012 2z"/></svg> Chat
      </button>
      <button class="sw-act-btn sa-rate sw-rate-btn" ${rateDataAttrs} style="display:none">
        <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg> Done &amp; Rate
      </button>
    </div>`;
  }
  return `<div class="sw-item">
    <div class="sw-item-top">
      <div class="sw-av" style="background:${otherColor||AVATAR_COLORS[0]}">${x(otherInit||'?')}</div>
      <div class="sw-info">
        <div class="sw-user">${x(otherName||'Unknown')}</div>
        <div class="sw-meta">Skill Swap &nbsp;·&nbsp; ${timeAgo(sw.createdAt)}</div>
      </div>
      <div class="sw-badge ${stMap[sw.status]||'st-pending'}">${capitalize(sw.status)}</div>
    </div>
    <div class="sw-skills">
      <div class="sw-skill"><div class="sw-skill-lbl t">You Teach</div><div class="sw-skill-val">${x(myTeach)}</div></div>
      <div class="sw-skill"><div class="sw-skill-lbl l">You Learn</div><div class="sw-skill-val">${x(theirTeach)}</div></div>
    </div>
    ${actions}
  </div>`;
}

/* ══════════════════════════════════════════════════════════════
   SWAP LIST — Delegated click handlers (fixes chat button bug)
══════════════════════════════════════════════════════════════ */
document.addEventListener('click', function(e) {
  // Chat button
  const chatBtn = e.target.closest('.sw-chat-btn');
  if (chatBtn) {
    const swId   = chatBtn.dataset.swid;
    const uid    = chatBtn.dataset.uid;
    const name   = chatBtn.dataset.name;
    const init   = chatBtn.dataset.init;
    const color  = chatBtn.dataset.color;
    if (swId && uid) openChat(swId, uid, name, init, color);
    return;
  }
  // Rate button
  const rateBtn = e.target.closest('.sw-rate-btn');
  if (rateBtn) {
    const swId = rateBtn.dataset.swid;
    const name = rateBtn.dataset.name;
    if (swId) openRating(swId, name);
    return;
  }
});

/* Check message count for a swap and show/hide Rate button accordingly */
async function checkAndUnlockRating(swapId) {
  try {
    const snap = await DB.collection('swaps').doc(swapId).collection('messages')
      .limit(10).get(); // only fetch up to 10 — we just need >= 3 unique exchanges
    const msgs = snap.docs.map(d => d.data());
    // Count distinct senders
    const senders = new Set(msgs.map(m => m.senderUid));
    // Require at least 3 total messages AND at least 2 different senders (real exchange)
    const qualifies = msgs.length >= 3 && senders.size >= 2;
    // Find the rate button for this swap and show/hide
    document.querySelectorAll(`.sw-rate-btn[data-swid="${swapId}"]`).forEach(btn => {
      btn.style.display = qualifies ? '' : 'none';
      if (qualifies) btn.title = '';
      else btn.title = 'Chat at least 3 messages to unlock rating';
    });
  } catch(e) { /* silent — just don't show button */ }
}

async function respondSwap(id, status) {
  try {
    await DB.collection('swaps').doc(id).update({ status });
    if (status === 'accepted') {
      const swSnap = await DB.collection('swaps').doc(id).get();
      const sw = swSnap.data();
      await DB.collection('notifications').add({
        toUid: sw.fromUid, fromUid: CU.uid, fromName: CUD?.name,
        type: 'swap_accepted',
        message: `${x(CUD?.name)} accepted your swap request! Open the chat to get started.`,
        read: false, createdAt: firebase?.firestore?.FieldValue.serverTimestamp(),
      });
      toast('Swap accepted! Open the chat to connect.','ok');
    } else { toast('Swap declined.'); }
    loadSwaps();
  } catch(e) { toast('Error responding. Try again.','er'); console.error(e); }
}

/* ══════════════════════════════════════════════════════════════
   RATING
══════════════════════════════════════════════════════════════ */
function openRating(swapId, name) {
  curSwapId = swapId;
  $('rateeName').textContent = name;
  starRating = 0;
  document.querySelectorAll('.star-btn').forEach(b=>b.classList.remove('on'));
  $('ovRating').classList.add('on');
}

function pickStar(n) {
  starRating = n;
  document.querySelectorAll('.star-btn').forEach((b,i) => b.classList.toggle('on', i<n));
}

async function submitRating() {
  if (!starRating) { toast('Please select a star rating'); return; }
  // Bug #16: Check if already rated
  const swSnap = await DB.collection('swaps').doc(curSwapId).get();
  const sw = swSnap.data();
  if (!sw) { toast('Swap not found','er'); return; }
  if (sw.myRating) { toast('You have already rated this swap.'); closeOv('ovRating'); return; }
  try {
    const swSnap = await DB.collection('swaps').doc(curSwapId).get();
    const sw = swSnap.data();
    if (!sw) { toast('Swap not found','er'); return; }
    const rateeUid = sw.fromUid===CU.uid ? sw.toUid : sw.fromUid;
    await DB.collection('swaps').doc(curSwapId).update({ status:'completed', myRating: starRating });
    await DB.collection('users').doc(rateeUid).update({
      rating: firebase?.firestore?.FieldValue.increment(starRating),
      ratingCount: firebase?.firestore?.FieldValue.increment(1),
      swapsCompleted: firebase?.firestore?.FieldValue.increment(1),
    });
    await DB.collection('users').doc(CU.uid).update({ swapsCompleted: firebase?.firestore?.FieldValue.increment(1) });
    await DB.collection('notifications').add({
      toUid: rateeUid, fromName: CUD?.name, type: 'rating',
      message: `${x(CUD?.name)} gave you ${starRating} stars! Great swap.`,
      read: false, createdAt: firebase?.firestore?.FieldValue.serverTimestamp(),
    });
    closeOv('ovRating');
    toast('Rating submitted!','ok');
    starRating = 0;
    loadSwaps();
  } catch(e) { toast('Error submitting. Try again.','er'); console.error(e); }
}

/* ══════════════════════════════════════════════════════════════
   LEADERBOARD
══════════════════════════════════════════════════════════════ */
async function loadLboard() {
  const el = $('lbList');
  el.innerHTML = '<div class="loader"><div class="spin-ring"></div> Loading…</div>';
  try {
    const snap = await DB.collection('users').get();
    const users = snap.docs.map(d=>d.data())
      .filter(u=>(u.swapsCompleted||0)>0)
      .sort((a,b)=>(b.swapsCompleted||0)-(a.swapsCompleted||0))
      .slice(0,30);
    if (!users.length) {
      el.innerHTML = '<div class="error-state">No completed swaps yet.<br>Be the first!</div>'; return;
    }
    el.innerHTML = users.map((u,i) => {
      const r = i+1;
      const medal = r===1?'🥇':r===2?'🥈':r===3?'🥉':String(r);
      const rc    = r===1?'rank1':r===2?'rank2':r===3?'rank3':'';
      const rat   = u.ratingCount>0 ? (u.rating/u.ratingCount).toFixed(1) : '—';
      const isMe  = u.uid===CU?.uid;
      return `<div class="lb-item${isMe?' lb-me':''}">
        <div class="lb-rank ${rc}">${medal}</div>
        <div class="lb-av" style="background:${u.avatarColor||AVATAR_COLORS[0]}">${x(u.initials||'?')}</div>
        <div class="lb-info">
          <div class="lb-name">${x(u.name)}${isMe?' (You)':''}</div>
          <div class="lb-skill">&#9733; ${rat} &nbsp;·&nbsp; ${x((u.teaches||['—'])[0])}</div>
        </div>
        <div class="lb-score"><div class="lb-num">${u.swapsCompleted}</div><div class="lb-numl">swaps</div></div>
      </div>`;
    }).join('');
  } catch(e) {
    el.innerHTML = `<div class="error-state">Error loading leaderboard.<br><small>${x(e.message)}</small></div>`;
  }
}

/* ══════════════════════════════════════════════════════════════
   SAVE PROFILE
══════════════════════════════════════════════════════════════ */
async function saveProfile() {
  const teaches = getPillValues('setupTeachPills','p-teach');
  const learns  = getPillValues('setupLearnPills','p-learn');
  const bio     = ($('setupBio').value||'').trim();
  if (!teaches.length) { toast('Select at least 1 skill to teach'); return; }
  if (!learns.length)  { toast('Select at least 1 skill to learn'); return; }
  $('saveTxt').textContent = 'Saving…';
  try {
    await DB.collection('users').doc(CU.uid).update({ teaches, learns, bio });
    CUD = { ...CUD, teaches, learns, bio };
    $('saveTxt').textContent = 'Save & Go Live';
    toast('Profile updated! You are live.','ok');
    loadExplore(); loadMatches();
    navTo('sHome', null);
  } catch(e) {
    $('saveTxt').textContent = 'Save & Go Live';
    toast('Could not save profile. Please check your connection and try again.','er');
    console.error(e);
  }
}

/* ══════════════════════════════════════════════════════════════
   PROFILE
══════════════════════════════════════════════════════════════ */
function refreshProfile() {
  if (!CUD || !CU) return;
  $('prAv').textContent = CUD.initials||'?';
  $('prName').textContent = CUD.name||'—';
  $('prEmail').textContent = CU.email||'—';
  $('prSwaps').textContent = CUD.swapsCompleted||0;
  $('prStreak').textContent = CUD.streak||0;
  const rat = CUD.ratingCount>0 ? (CUD.rating/CUD.ratingCount).toFixed(1) : '—';
  $('prRating').textContent = rat==='—' ? '—' : '★ '+rat;
  $('prTeach').innerHTML = (CUD.teaches||[]).length
    ? (CUD.teaches).map(s=>`<div class="skill-item"><div class="skill-dot dot-a"></div>${x(s)}</div>`).join('')
    : '<div style="font-size:12px;color:var(--txt3)">None set</div>';
  $('prLearn').innerHTML = (CUD.learns||[]).length
    ? (CUD.learns).map(s=>`<div class="skill-item"><div class="skill-dot dot-t"></div>${x(s)}</div>`).join('')
    : '<div style="font-size:12px;color:var(--txt3)">None set</div>';
  // Settings screen
  const se = $('settingsEmail'); if(se) se.textContent = CU.email||'—';
  // Sync theme toggle state
  const tt = $('themeToggle'); if(tt) tt.checked = !isDark;
}

/* ══════════════════════════════════════════════════════════════
   NOTIFICATIONS
══════════════════════════════════════════════════════════════ */
function startNotifListener() {
  if (!CU) return;
  DB.collection('notifications').where('toUid','==',CU.uid).where('read','==',false)
    .onSnapshot(snap => {
      const n = snap.size;
      const dot = $('notifDot'); if(dot) dot.style.display = n>0?'block':'none';
      // Bug #17: Use separate badge for notifications (bell) vs swaps (tab)
      const notifBadge = $('notifBadge'); if(notifBadge&&n>0){notifBadge.textContent=n;notifBadge.style.display='block';}
      else if(notifBadge) notifBadge.style.display='none';
    }, e => console.warn('notif listener:', e.code));
}

async function loadNotifs() {
  const el = $('notifList');
  el.innerHTML = '<div class="loader"><div class="spin-ring"></div></div>';
  if (!CU) return;
  try {
    // Bug #4: Fallback if composite index doesn't exist yet
    let snap;
    try {
      snap = await DB.collection('notifications')
        .where('toUid','==',CU.uid)
        .orderBy('createdAt','desc')
        .limit(40).get();
    } catch(indexErr) {
      // Fallback: no orderBy, sort client-side
      snap = await DB.collection('notifications')
        .where('toUid','==',CU.uid)
        .limit(40).get();
      if (indexErr.code === 'failed-precondition') {
        console.warn('loadNotifs: Missing Firestore index. Check console for index creation link.');
      }
    }
    const notifs = snap.docs.map(d=>({id:d.id,...d.data()}))
      .sort((a,b)=>(b.createdAt?.seconds||0)-(a.createdAt?.seconds||0));
    if (!notifs.length) {
      el.innerHTML = '<div class="error-state">No notifications yet.</div>'; return;
    }
    const typeIcon = {
      swap_request: `<svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><polyline points="17 1 21 5 17 9"/><path d="M3 11V9a4 4 0 014-4h14"/><polyline points="7 23 3 19 7 15"/><path d="M21 13v2a4 4 0 01-4 4H3"/></svg>`,
      swap_accepted:`<svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="var(--green)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>`,
      rating:       `<svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="var(--yellow)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>`,
      incoming_call:`<svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="var(--blue)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M15 10l4.553-2.069A1 1 0 0121 8.87v6.26a1 1 0 01-1.447.9L15 14M3 8a2 2 0 012-2h8a2 2 0 012 2v8a2 2 0 01-2 2H5a2 2 0 01-2-2V8z"/></svg>`,
    };
    const typeBg = { swap_request:'background:var(--adim)', swap_accepted:'background:rgba(39,174,96,.10)', rating:'background:rgba(240,180,41,.10)', incoming_call:'background:rgba(79,142,247,.10)' };
    el.innerHTML = notifs.map(n=>`
      <div class="notif-item${n.read?'':' unread'}">
        <div class="notif-ic" style="${typeBg[n.type]||'background:var(--adim)'}">${typeIcon[n.type]||typeIcon.swap_request}</div>
        <div class="notif-body">
          <div class="notif-text">${x(n.message||'')}</div>
          ${n.swapMessage ? `<div class="notif-swap-msg">"${x(n.swapMessage)}"</div>` : ''}
          <div class="notif-time">${timeAgo(n.createdAt)}</div>
        </div>
        ${!n.read?'<div class="notif-unread-dot"></div>':''}
      </div>`).join('');
    // Mark all read
    const batch = DB.batch();
    snap.docs.filter(d=>!d.data().read).forEach(d=>batch.update(d.ref,{read:true}));
    batch.commit().catch(()=>{});
  } catch(e) {
    if (e.code === 'failed-precondition') {
      el.innerHTML = '<div class="error-state">Notifications require a Firestore index.<br><small>Check console for index creation link.</small></div>';
    } else {
      el.innerHTML = `<div class="error-state">Error loading notifications.<br><small>${x(e.message)}</small></div>`;
    }
    console.error('loadNotifs:', e.code, e.message, e);
  }
}

/* ══════════════════════════════════════════════════════════════
   REPORT
══════════════════════════════════════════════════════════════ */
function openReport() {
  reportReason = '';
  document.querySelectorAll('.report-option').forEach(r=>r.classList.remove('on'));
  $('reportDetail').value = '';
  $('ovReport').classList.add('on');
}
function selectReport(el, reason) {
  document.querySelectorAll('.report-option').forEach(r=>r.classList.remove('on'));
  el.classList.add('on'); reportReason = reason;
}
async function submitReport() {
  if (!reportReason) { toast('Please select a reason'); return; }
  try {
    await DB.collection('reports').add({
      reportedUid: detailUser?.uid||'unknown',
      reportedName: detailUser?.name||'Unknown',
      reporterUid: CU?.uid,
      reason: reportReason,
      details: ($('reportDetail').value||'').trim(),
      status: 'pending',
      createdAt: firebase?.firestore?.FieldValue.serverTimestamp(),
    });
    closeOv('ovReport');
    toast('Report submitted. We will review within 24h.','ok');
  } catch(e) { toast('Error submitting report.','er'); }
}

/* ══════════════════════════════════════════════════════════════
   WEBRTC INCOMING CALL (Bug #12)
══════════════════════════════════════════════════════════════ */
let _incomingCallUnsubscribe = null;

function startIncomingCallListener() {
  if (!CU) return;
  // Clean up any previous listener
  if (_incomingCallUnsubscribe) { _incomingCallUnsubscribe(); _incomingCallUnsubscribe = null; }
  _incomingCallUnsubscribe = DB.collection('calls')
    .where('calledUid','==',CU.uid)
    .onSnapshot(async snap => {
      snap.docChanges().forEach(async change => {
        if (change.type === 'added') {
          const data = change.doc.data();
          if (data.offer && !data.answer) {
            const nameEl = $('incomingCallName');
            if(nameEl) nameEl.textContent = data.callerName || 'Someone';
            const ov = $('ovIncomingCall');
            if(ov) ov.classList.add('on');
            window._pendingCallId = change.doc.id;
            window._pendingCallData = data;
          }
        }
      });
    }, ()=>{});
}

async function answerCall() {
  const swapId = window._pendingCallId;
  const callData = window._pendingCallData;
  const ov = $('ovIncomingCall'); if(ov) ov.classList.remove('on');
  const callNameEl = $('callName'); if(callNameEl) callNameEl.textContent = callData.callerName || 'Caller';
  const callScr = $('callScr'); if(callScr) callScr.classList.add('on');
  try {
    localStream = await navigator.mediaDevices.getUserMedia({video:true,audio:true});
    const lv = $('locVid'); if(lv) lv.srcObject = localStream;
    peerConn = new RTCPeerConnection(ICE_SERVERS);
    localStream.getTracks().forEach(t=>peerConn.addTrack(t, localStream));
    peerConn.ontrack = e => {
      const rv = $('remVid'); if(rv) rv.srcObject = e.streams[0];
      const cc = $('callConnecting'); if(cc) cc.classList.add('hidden');
      startCallTimer();
    };
    peerConn.onicecandidate = e => {
      if (e.candidate) DB.collection('calls').doc(swapId).collection('ice_callee').add(e.candidate.toJSON()).catch(()=>{});
    };
    await peerConn.setRemoteDescription(new RTCSessionDescription(callData.offer));
    const answer = await peerConn.createAnswer();
    await peerConn.setLocalDescription(answer);
    await DB.collection('calls').doc(swapId).update({ answer: {type:answer.type,sdp:answer.sdp} });
    DB.collection('calls').doc(swapId).collection('ice_caller').onSnapshot(snap => {
      snap.docChanges().forEach(ch => {
        if (ch.type==='added' && peerConn) peerConn.addIceCandidate(new RTCIceCandidate(ch.doc.data())).catch(()=>{});
      });
    });
  } catch(e) {
    toast('Could not start call.','er');
    console.error('answerCall:', e.code, e.message, e);
    endCall();
  }
}

function declineCall() {
  const ov = $('ovIncomingCall'); if(ov) ov.classList.remove('on');
  window._pendingCallId = null;
  window._pendingCallData = null;
  toast('Call declined.');
}

/* ══════════════════════════════════════════════════════════════
   CHAT
══════════════════════════════════════════════════════════════ */
let chatSwapId = null, chatOtherUid = null, chatUnsub = null;

function openChat(swapId, otherUid, otherName, otherInit, otherColor) {
  chatSwapId = swapId;
  chatOtherUid = otherUid;
  $('chatName').textContent = otherName || '—';
  const av = $('chatAv');
  av.textContent = otherInit || '?';
  av.style.background = otherColor || AVATAR_COLORS[0];
  navTo('sChat', null);
  subscribeChatMsgs(swapId);
}

function subscribeChatMsgs(swapId) {
  if (chatUnsub) { chatUnsub(); chatUnsub = null; }
  const container = $('chatMsgs');
  container.innerHTML = '<div class="loader"><div class="spin-ring"></div></div>';
  // No .orderBy() here — avoids requiring a Firestore composite index.
  // Messages are sorted client-side by createdAt ascending instead.
  chatUnsub = DB.collection('swaps').doc(swapId).collection('messages')
    .onSnapshot(snap => {
      const msgs = snap.docs
        .map(d => ({id: d.id, ...d.data()}))
        .sort((a, b) => {
          const aTs = a.createdAt?.seconds ?? (a.createdAt?.toMillis ? a.createdAt.toMillis()/1000 : 0);
          const bTs = b.createdAt?.seconds ?? (b.createdAt?.toMillis ? b.createdAt.toMillis()/1000 : 0);
          return aTs - bTs;
        });
      renderChatMsgs(msgs);
      // Re-check rating unlock on every message update
      checkAndUnlockRating(swapId).catch(()=>{});
    }, e => {
      container.innerHTML = '<div class="error-state">Could not load messages.<br><small>Check your internet connection and try again.</small></div>';
      console.error('chat snapshot error:', e.code, e.message);
    });
}

function renderChatMsgs(msgs) {
  const container = $('chatMsgs');
  if (!msgs.length) {
    container.innerHTML = `<div class="chat-empty">
      <div class="chat-empty-icon"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15a2 2 0 01-2 2H7l-4 4V5a2 2 0 012-2h14a2 2 0 012 2z"/></svg></div>
      <div class="chat-empty-txt">Say hello! Start coordinating your skill swap.</div>
    </div>`;
    return;
  }
  let lastDate = '';
  container.innerHTML = msgs.map(msg => {
    const mine = msg.senderUid === CU?.uid;
    const ts = msg.createdAt?.toDate ? msg.createdAt.toDate() : new Date();
    const dateStr = ts.toLocaleDateString([], {month:'short', day:'numeric'});
    const timeStr = ts.toLocaleTimeString([], {hour:'2-digit', minute:'2-digit'});
    let dateSep = '';
    if (dateStr !== lastDate) { lastDate = dateStr; dateSep = `<div class="chat-date-sep">${dateStr}</div>`; }
    return `${dateSep}<div class="chat-bubble-wrap${mine?' mine':''}">
      <div class="chat-bubble">${x(msg.text)}</div>
      <div class="chat-time">${timeStr}</div>
    </div>`;
  }).join('');
  container.scrollTop = container.scrollHeight;
}

async function sendChatMsg() {
  if (!CU || !chatSwapId) return;
  const inp = $('chatInp');
  const text = inp.value.trim();
  if (!text) return;
  // Save text first — only clear after successful write so message is never lost on failure
  const sendBtn = $('chatSendBtn');
  if (sendBtn) sendBtn.disabled = true;
  try {
    await DB.collection('swaps').doc(chatSwapId).collection('messages').add({
      text,
      senderUid: CU.uid,
      senderName: CUD?.name || '',
      createdAt: firebase?.firestore?.FieldValue.serverTimestamp(),
    });
    // Clear input only after successful send
    inp.value = '';
    inp.style.height = '';
    // Re-check rating unlock after each message sent
    checkAndUnlockRating(chatSwapId).catch(()=>{});
  } catch(e) {
    // Don't clear input — keep the user's text so they can retry
    toast('Could not send message. Try again.', 'er');
    console.error('sendChatMsg:', e.code, e.message);
  } finally {
    if (sendBtn) sendBtn.disabled = false;
  }
}

function autoResizeChatInp(el) {
  el.style.height = 'auto';
  el.style.height = Math.min(el.scrollHeight, 100) + 'px';
}

function chatEnterSend(e) {
  if (e.key === 'Enter' && !e.shiftKey) {
    e.preventDefault();
    sendChatMsg();
  }
}

/* ══════════════════════════════════════════════════════════════
   WEBRTC VIDEO CALL
══════════════════════════════════════════════════════════════ */
const ICE_SERVERS = { iceServers:[{urls:'stun:stun.l.google.com:19302'},{urls:'stun:stun1.l.google.com:19302'}] };

async function startCall(swapId, remoteUid, remoteName) {
  curSwapId = swapId;
  $('callName').textContent = remoteName;
  $('callConnName').textContent = `Calling ${remoteName}…`;
  $('callConnecting').classList.remove('hidden');
  $('callScr').classList.add('on');
  try {
    localStream = await navigator.mediaDevices.getUserMedia({video:true,audio:true});
    $('locVid').srcObject = localStream;
    peerConn = new RTCPeerConnection(ICE_SERVERS);
    localStream.getTracks().forEach(t=>peerConn.addTrack(t, localStream));
    peerConn.ontrack = e => {
      $('remVid').srcObject = e.streams[0];
      $('callConnecting').classList.add('hidden');
      $('callStatus').textContent = 'Connected';
      startCallTimer();
    };
    peerConn.onicecandidate = e => {
      if (e.candidate) {
        DB.collection('calls').doc(swapId).collection('ice_caller').add(e.candidate.toJSON()).catch(()=>{});
      }
    };
    peerConn.onconnectionstatechange = () => {
      if (['disconnected','failed','closed'].includes(peerConn?.connectionState)) endCall();
    };
    const offer = await peerConn.createOffer();
    await peerConn.setLocalDescription(offer);
    await DB.collection('calls').doc(swapId).set({ offer:{type:offer.type,sdp:offer.sdp}, callerUid:CU.uid, callerName:CUD?.name||'', calledUid:remoteUid });
    // Listen for answer
    DB.collection('calls').doc(swapId).onSnapshot(async snap => {
      const d = snap.data();
      if (d?.answer && peerConn && !peerConn.currentRemoteDescription) {
        await peerConn.setRemoteDescription(new RTCSessionDescription(d.answer)).catch(()=>{});
      }
    });
    // Remote ICE candidates
    DB.collection('calls').doc(swapId).collection('ice_callee').onSnapshot(snap => {
      snap.docChanges().forEach(ch => {
        if (ch.type==='added' && peerConn) peerConn.addIceCandidate(new RTCIceCandidate(ch.doc.data())).catch(()=>{});
      });
    });
    // Notify other user
    await DB.collection('notifications').add({
      toUid: remoteUid, fromUid: CU.uid, fromName: CUD?.name,
      type: 'incoming_call', swapId,
      message: `${x(CUD?.name)} is calling you! Open your Swaps tab.`,
      read: false, createdAt: firebase?.firestore?.FieldValue.serverTimestamp(),
    });
  } catch(e) {
    toast('Camera or microphone not available.','er');
    endCall(); console.error('startCall:', e);
  }
}

function toggleMic() {
  micMuted = !micMuted;
  localStream?.getAudioTracks().forEach(t=>t.enabled=!micMuted);
  $('btnMic').classList.toggle('off', micMuted);
}
function toggleCam() {
  camOff = !camOff;
  localStream?.getVideoTracks().forEach(t=>t.enabled=!camOff);
  $('btnCam').classList.toggle('off', camOff);
}
function endCall() {
  try { peerConn?.close(); } catch(e){}
  peerConn = null;
  localStream?.getTracks().forEach(t=>t.stop());
  localStream = null;
  const rv=$('remVid'), lv=$('locVid');
  if(rv) rv.srcObject=null;
  if(lv) lv.srcObject=null;
  $('callScr').classList.remove('on');
  $('callConnecting').classList.remove('hidden');
  $('callStatus').textContent = 'Connecting…';
  clearInterval(callTimerInt); callSecs=0;
  $('callTimer').textContent='00:00';
  micMuted=false; camOff=false;
  toast('Call ended');
}
function startCallTimer() {
  clearInterval(callTimerInt);
  callTimerInt = setInterval(()=>{
    callSecs++;
    const m = String(Math.floor(callSecs/60)).padStart(2,'0');
    const s = String(callSecs%60).padStart(2,'0');
    $('callTimer').textContent = `${m}:${s}`;
  },1000);
}

/* ══════════════════════════════════════════════════════════════
   OVERLAYS / SHEET HELPERS
══════════════════════════════════════════════════════════════ */
function closeOv(id) { const el=$(id); if(el) el.classList.remove('on'); }

/* ══════════════════════════════════════════════════════════════
   TOAST
══════════════════════════════════════════════════════════════ */
let _toastTimer;
function toast(msg, type='') {
  const el = $('toast');
  el.textContent = msg;
  el.className = 'show' + (type ? ' '+type : '');
  clearTimeout(_toastTimer);
  _toastTimer = setTimeout(()=>el.className='', 3200);
}

/* ══════════════════════════════════════════════════════════════
   UTILITY FUNCTIONS
══════════════════════════════════════════════════════════════ */
// Safe DOM getter
function $(id) { return document.getElementById(id); }
// Get input value
function $v(id) { const el=$(id); return el ? el.value.trim() : ''; }
// XSS-safe text escaping
function x(s) {
  if (!s) return '';
  return String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#39;');
}
// Time ago
function timeAgo(ts) {
  if (!ts) return '';
  const secs = Math.floor((Date.now() - (ts.seconds?ts.seconds*1000:ts)) / 1000);
  if (secs < 60) return 'just now';
  if (secs < 3600) return Math.floor(secs/60)+'m ago';
  if (secs < 86400) return Math.floor(secs/3600)+'h ago';
  return Math.floor(secs/86400)+'d ago';
}
// Format large numbers
function fmtNum(n) { return n>=1000 ? (n/1000).toFixed(1)+'k' : String(n); }
// Capitalize first letter
function capitalize(s) { return s ? s[0].toUpperCase()+s.slice(1) : ''; }

// Set button loading state
function setBtn(btnId, txtId, txt, disabled) {
  const btn=$(btnId), span=$(txtId);
  if(disabled) {
    if(span) span.textContent = txt;
    if(btn) btn.disabled = true;
  } else {
    if(span) span.textContent = txt;
    if(btn) btn.disabled = false;
  }
}

/* ══════════════════════════════════════════════════════════════
   KEYBOARD SHORTCUTS
══════════════════════════════════════════════════════════════ */
document.getElementById('lPass').addEventListener('keydown', e=>{ if(e.key==='Enter') doLogin(); });
document.getElementById('sPass').addEventListener('keydown', e=>{ if(e.key==='Enter') doSignup(); });
document.getElementById('lEmail').addEventListener('keydown', e=>{ if(e.key==='Enter') $('lPass').focus(); });
document.getElementById('sEmail').addEventListener('keydown', e=>{ if(e.key==='Enter') $('sPass').focus(); });
document.getElementById('sName').addEventListener('keydown', e=>{ if(e.key==='Enter') $('sEmail').focus(); });

/* ══════════════════════════════════════════════════════════════
   PWA — SERVICE WORKER & INSTALL PROMPT
══════════════════════════════════════════════════════════════ */

// ── Register Service Worker (relative path — works on any host) ──
if ('serviceWorker' in navigator) {
  window.addEventListener('load', () => {
    // Skip SW registration in sandboxed iframes (Claude preview, etc.) — sw.js won't exist there
    const isSandboxed = window.location.hostname.includes('claudeusercontent.com') ||
                        window.location.hostname.includes('codesandbox') ||
                        window.location.protocol === 'blob:';
    if (!isSandboxed) {
      navigator.serviceWorker.register('./sw.js', { scope: './' })
        .then(reg => console.log('SW registered:', reg.scope))
        .catch(() => {}); // silently skip if sw.js not present
    }
  });
}

// ── Already running as installed PWA? Hide banner forever ──
const _isStandalone =
  window.matchMedia('(display-mode: standalone)').matches ||
  window.navigator.standalone === true;

if (_isStandalone) {
  try { localStorage.setItem('ss_pwa_installed', '1'); } catch(e) {}
}

// ── State ────────────────────────────────────────────────────
let _deferredInstallPrompt = null;
const _banner     = document.getElementById('pwaInstallBanner');
const _installBtn = document.getElementById('pwaInstallBtn');
const _dismissBtn = document.getElementById('pwaDismissBtn');

function _pwaHandled() {
  try {
    return localStorage.getItem('ss_pwa_installed') === '1' ||
           localStorage.getItem('ss_pwa_dismissed') === '1';
  } catch(e) { return false; }
}

function _showBanner() {
  if (_pwaHandled() || _isStandalone || !_banner) return;
  // Small delay so it doesn't pop up during splash screen
  setTimeout(() => {
    if (!_pwaHandled()) _banner.classList.add('show');
  }, 2500);
}

// ── Chrome / Android — beforeinstallprompt ───────────────────
// This is the standard event. Chrome fires it when all PWA
// criteria are met (HTTPS, manifest, SW, valid icons).
window.addEventListener('beforeinstallprompt', e => {
  e.preventDefault(); // stop mini-infobar
  _deferredInstallPrompt = e;
  console.log('PWA: beforeinstallprompt fired ✓');
  _showBanner();
});

// ── Install button clicked ───────────────────────────────────
if (_installBtn) {
  _installBtn.addEventListener('click', async () => {
    _banner.classList.remove('show');

    if (_deferredInstallPrompt) {
      // Android Chrome — native install dialog
      _deferredInstallPrompt.prompt();
      const { outcome } = await _deferredInstallPrompt.userChoice;
      _deferredInstallPrompt = null;
      if (outcome === 'accepted') {
        try { localStorage.setItem('ss_pwa_installed', '1'); } catch(e) {}
        toast('SwapSkill installed! 🎉', 'ok');
      } else {
        try { localStorage.setItem('ss_pwa_dismissed', '1'); } catch(e) {}
      }
    } else {
      // iOS Safari — no API, show manual instructions
      toast('Tap the Share button → "Add to Home Screen"');
      try { localStorage.setItem('ss_pwa_dismissed', '1'); } catch(e) {}
    }
  });
}

// ── Dismiss button clicked ───────────────────────────────────
if (_dismissBtn) {
  _dismissBtn.addEventListener('click', () => {
    _banner.classList.remove('show');
    try { localStorage.setItem('ss_pwa_dismissed', '1'); } catch(e) {}
  });
}

// ── After successful install via browser prompt ──────────────
window.addEventListener('appinstalled', () => {
  try { localStorage.setItem('ss_pwa_installed', '1'); } catch(e) {}
  if (_banner) _banner.classList.remove('show');
  toast('SwapSkill installed! 🎉', 'ok');
});

</script>
</body>
</html>
